# 🎞️ 필름랩 (FilmLab) — 상세 개발 설계서

**버전**: 1.0  
**작성일**: 2026년 1월 27일  
**예상 개발기간**: 12주 (3개월)

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택](#2-기술-스택)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [데이터베이스 설계](#4-데이터베이스-설계)
5. [핵심 모듈 설계](#5-핵심-모듈-설계)
6. [UI/UX 설계](#6-uiux-설계)
7. [필름 프리셋 시스템](#7-필름-프리셋-시스템)
8. [현상 시스템](#8-현상-시스템)
9. [API 설계](#9-api-설계)
10. [파일 구조](#10-파일-구조)
11. [개발 일정](#11-개발-일정)
12. [테스트 계획](#12-테스트-계획)
13. [출시 체크리스트](#13-출시-체크리스트)

---

## 1. 프로젝트 개요

### 1.1 제품 정의
| 항목 | 내용 |
|------|------|
| 앱 이름 | 필름랩 (FilmLab) |
| 플랫폼 | iOS 15+ / Android 11+ |
| 카테고리 | Photo & Video |
| 타겟 | 20-35세, 레트로 감성 선호 |
| 핵심 가치 | "기다림의 미학, 우연의 아름다움" |

### 1.2 MVP 기능 범위
```
✅ MVP 포함
- 필름 카메라 촬영
- 필름 롤 시스템 (24장)
- 필름 프리셋 5종
- 현상 대기 시스템
- 갤러리 & 앨범
- 기본 공유 기능

❌ MVP 제외 (v2.0)
- 영상 촬영
- 프린트 연동
- 소셜 기능
- 프리미엄 필름
```

### 1.3 성공 지표 (KPI)
| 지표 | 목표 |
|------|------|
| Day 1 Retention | 40%+ |
| Day 7 Retention | 20%+ |
| 월간 구독 전환율 | 5%+ |
| 평균 세션 시간 | 3분+ |
| 앱스토어 평점 | 4.5+ |

---

## 2. 기술 스택

### 2.1 iOS
```yaml
Language: Swift 5.9+
UI Framework: SwiftUI + UIKit (Camera)
Minimum iOS: 15.0
Architecture: MVVM + Clean Architecture

Dependencies:
  - Camera: AVFoundation
  - Image Processing: Core Image, Metal
  - Storage: Core Data + FileManager
  - Networking: URLSession (native)
  - Analytics: Firebase Analytics
  - Crash: Firebase Crashlytics
  - Ads: Google AdMob
  - IAP: StoreKit 2
```

### 2.2 Android
```yaml
Language: Kotlin 1.9+
UI Framework: Jetpack Compose
Minimum SDK: 30 (Android 11)
Target SDK: 34 (Android 14)
Architecture: MVVM + Clean Architecture

Dependencies:
  - Camera: CameraX
  - Image Processing: RenderScript, GPUImage
  - Storage: Room + FileManager
  - Networking: Retrofit + OkHttp
  - DI: Hilt
  - Analytics: Firebase Analytics
  - Crash: Firebase Crashlytics
  - Ads: Google AdMob
  - IAP: Google Play Billing
```

### 2.3 공통/백엔드
```yaml
Backend: Firebase (Serverless)
  - Auth: Firebase Auth (Anonymous + Apple/Google)
  - Database: Firestore
  - Storage: Firebase Storage (선택)
  - Functions: Cloud Functions (현상 푸시)
  - Remote Config: A/B 테스트

CI/CD:
  - iOS: Fastlane + GitHub Actions
  - Android: Fastlane + GitHub Actions
```

---

## 3. 시스템 아키텍처

### 3.1 전체 구조
```
┌─────────────────────────────────────────────────────┐
│                    Presentation Layer                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │   Camera    │  │   Gallery   │  │  Settings   │  │
│  │    View     │  │    View     │  │    View     │  │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │
└─────────┼────────────────┼────────────────┼─────────┘
          │                │                │
┌─────────┼────────────────┼────────────────┼─────────┐
│         ▼                ▼                ▼         │
│                    ViewModel Layer                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │   Camera    │  │   Gallery   │  │  Settings   │  │
│  │  ViewModel  │  │  ViewModel  │  │  ViewModel  │  │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │
└─────────┼────────────────┼────────────────┼─────────┘
          │                │                │
┌─────────┼────────────────┼────────────────┼─────────┐
│         ▼                ▼                ▼         │
│                     Domain Layer                     │
│  ┌─────────────────────────────────────────────────┐│
│  │  UseCases: CapturePhoto, ProcessFilm,           ││
│  │            DevelopFilm, GetPhotos, etc.         ││
│  └─────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────┐│
│  │  Entities: FilmRoll, Photo, FilmPreset, etc.    ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
          │
┌─────────┼───────────────────────────────────────────┐
│         ▼              Data Layer                    │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │  Repository  │  │   Local DB   │  │   Remote   │ │
│  │    Impl      │  │  (CoreData)  │  │  (Firebase)│ │
│  └──────────────┘  └──────────────┘  └────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 3.2 카메라 파이프라인
```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Camera  │───▶│  Preview │───▶│  Capture │───▶│  Filter  │
│  Input   │    │  Layer   │    │  Buffer  │    │  Apply   │
└──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                      │
┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  Gallery │◀───│  Develop │◀───│   Save   │◀────────┘
│  Display │    │  Process │    │  to Disk │
└──────────┘    └──────────┘    └──────────┘
```

---

## 4. 데이터베이스 설계

### 4.1 Core Data / Room Schema

```swift
// FilmRoll Entity
@Entity
class FilmRoll {
    @PrimaryKey var id: UUID
    var filmPresetId: String      // 필름 종류
    var maxPhotos: Int            // 24 or 36
    var currentCount: Int         // 현재 촬영 장수
    var status: RollStatus        // shooting, developing, developed
    var developStartTime: Date?   // 현상 시작 시간
    var developDuration: Int      // 현상 소요 시간 (초)
    var createdAt: Date
    var developedAt: Date?
    
    @Relationship var photos: [Photo]
}

// Photo Entity
@Entity
class Photo {
    @PrimaryKey var id: UUID
    var rollId: UUID              // FK to FilmRoll
    var originalPath: String      // 원본 이미지 경로
    var processedPath: String?    // 현상 후 이미지 경로
    var thumbnailPath: String?    // 썸네일 경로
    var frameNumber: Int          // 프레임 번호 (1-24)
    var capturedAt: Date
    var metadata: PhotoMetadata   // EXIF 등
    var defects: [DefectType]     // 적용된 결함들
}

// FilmPreset Entity (앱 번들에 포함)
struct FilmPreset: Codable {
    var id: String
    var name: String              // "Kodak Gold 200"
    var category: FilmCategory    // color, bw, cinema
    var isPremium: Bool
    var filterParams: FilterParams
    var defectProbability: DefectConfig
    var thumbnailName: String
}

// Enums
enum RollStatus: String {
    case shooting     // 촬영 중
    case readyToDev   // 현상 대기
    case developing   // 현상 중
    case developed    // 현상 완료
}

enum DefectType: String {
    case lightLeak    // 광선 누출
    case dust         // 먼지
    case scratch      // 스크래치
    case colorShift   // 색 바램
    case vignette     // 비네팅
    case grain        // 필름 그레인
}
```

### 4.2 Firestore Schema (선택적 동기화)
```javascript
// users/{userId}
{
  createdAt: Timestamp,
  isPremium: boolean,
  premiumExpiry: Timestamp,
  settings: {
    defaultFilm: string,
    developDuration: number
  }
}

// users/{userId}/rolls/{rollId}
{
  filmPresetId: string,
  photoCount: number,
  status: string,
  createdAt: Timestamp,
  developedAt: Timestamp
}
```

---

## 5. 핵심 모듈 설계

### 5.1 CameraModule

```swift
// CameraManager.swift
protocol CameraManagerProtocol {
    var previewLayer: AVCaptureVideoPreviewLayer { get }
    var currentFilm: FilmPreset { get set }
    
    func startSession()
    func stopSession()
    func capturePhoto() async throws -> CapturedPhoto
    func switchCamera()
    func toggleFlash()
    func applyLiveFilter(_ preset: FilmPreset)
}

class CameraManager: CameraManagerProtocol {
    private let captureSession = AVCaptureSession()
    private let photoOutput = AVCapturePhotoOutput()
    private var currentDevice: AVCaptureDevice?
    
    // Metal을 사용한 실시간 필터 적용
    private let metalDevice = MTLCreateSystemDefaultDevice()
    private var ciContext: CIContext?
    
    func capturePhoto() async throws -> CapturedPhoto {
        // 1. AVCapturePhoto 획득
        // 2. CIImage로 변환
        // 3. 필름 필터 적용
        // 4. 메타데이터 추가
        // 5. 반환
    }
}
```

### 5.2 FilterEngine

```swift
// FilterEngine.swift
class FilmFilterEngine {
    private let context: CIContext
    
    struct FilterParams: Codable {
        var temperature: Float      // 색온도 (-1.0 ~ 1.0)
        var tint: Float            // 틴트
        var saturation: Float      // 채도
        var contrast: Float        // 대비
        var exposure: Float        // 노출
        var highlights: Float      // 하이라이트
        var shadows: Float         // 섀도우
        var grain: GrainParams     // 그레인
        var vignette: VignetteParams
        var colorCurve: ColorCurveParams
    }
    
    func applyFilmLook(to image: CIImage, preset: FilmPreset) -> CIImage {
        var result = image
        
        // 1. 색온도 조정
        result = applyTemperature(result, preset.filterParams.temperature)
        
        // 2. 톤 커브 적용
        result = applyColorCurve(result, preset.filterParams.colorCurve)
        
        // 3. 채도/대비 조정
        result = adjustSaturationContrast(result, preset.filterParams)
        
        // 4. 필름 그레인 추가
        result = addFilmGrain(result, preset.filterParams.grain)
        
        // 5. 비네팅
        result = applyVignette(result, preset.filterParams.vignette)
        
        return result
    }
    
    func applyDefects(to image: CIImage, defects: [DefectType]) -> CIImage {
        var result = image
        
        for defect in defects {
            switch defect {
            case .lightLeak:
                result = applyLightLeak(result)
            case .dust:
                result = applyDustParticles(result)
            case .scratch:
                result = applyScratches(result)
            case .colorShift:
                result = applyColorShift(result)
            default:
                break
            }
        }
        
        return result
    }
}
```

### 5.3 DevelopmentManager

```swift
// DevelopmentManager.swift
class DevelopmentManager {
    private let notificationManager: NotificationManager
    private let photoProcessor: PhotoProcessor
    
    // 현상 시작
    func startDevelopment(roll: FilmRoll, duration: DevelopDuration) async {
        // 1. 상태 업데이트
        roll.status = .developing
        roll.developStartTime = Date()
        roll.developDuration = duration.seconds
        
        // 2. 로컬 노티피케이션 예약
        await notificationManager.scheduleDevelopmentComplete(
            rollId: roll.id,
            at: Date().addingTimeInterval(TimeInterval(duration.seconds))
        )
        
        // 3. 저장
        await repository.save(roll)
    }
    
    // 현상 완료 체크
    func checkDevelopmentStatus(roll: FilmRoll) -> DevelopmentStatus {
        guard let startTime = roll.developStartTime else {
            return .notStarted
        }
        
        let elapsed = Date().timeIntervalSince(startTime)
        let remaining = TimeInterval(roll.developDuration) - elapsed
        
        if remaining <= 0 {
            return .ready
        } else {
            return .inProgress(remainingSeconds: Int(remaining))
        }
    }
    
    // 현상 완료 처리
    func completeDevelopment(roll: FilmRoll) async throws {
        // 1. 각 사진에 최종 필터 + 결함 적용
        for photo in roll.photos {
            let processed = try await photoProcessor.processForDevelopment(
                photo: photo,
                preset: roll.filmPreset
            )
            photo.processedPath = processed.path
        }
        
        // 2. 상태 업데이트
        roll.status = .developed
        roll.developedAt = Date()
        
        // 3. 저장
        await repository.save(roll)
    }
}

enum DevelopDuration {
    case instant    // 광고 시청
    case quick      // 1시간
    case standard   // 3시간
    case vintage    // 24시간
    
    var seconds: Int {
        switch self {
        case .instant: return 0
        case .quick: return 3600
        case .standard: return 10800
        case .vintage: return 86400
        }
    }
}
```

---

## 6. UI/UX 설계

### 6.1 화면 구조
```
App
├── 📷 CameraView (Main)
│   ├── ViewfinderView
│   ├── FilmCounterView
│   ├── ControlsView (Flash, Timer, Film Select)
│   └── ShutterButton
│
├── 🎞️ GalleryView
│   ├── RollsListView
│   │   └── RollCardView (with development status)
│   └── RollDetailView
│       └── FilmStripView
│
├── ⏳ DevelopingView
│   ├── ProgressIndicator
│   ├── TimeRemainingLabel
│   └── AdWatchButton (skip)
│
├── ⚙️ SettingsView
│   ├── FilmCollectionView
│   ├── SubscriptionView
│   └── AboutView
│
└── 🛒 StoreView
    ├── SubscriptionPlansView
    └── FilmPacksView
```

### 6.2 카메라 화면 레이아웃
```
┌─────────────────────────────────┐
│  [Film: Kodak Gold]    [⚙️]    │  ← 상단바
├─────────────────────────────────┤
│                                 │
│    ┌───────────────────────┐    │
│    │                       │    │
│    │                       │    │
│    │     Viewfinder        │    │  ← 뷰파인더 (4:3)
│    │   (Live Filter)       │    │
│    │                       │    │
│    │                       │    │
│    └───────────────────────┘    │
│                                 │
│         [ 12 / 24 ]             │  ← 필름 카운터
│                                 │
├─────────────────────────────────┤
│  [⚡]   [📷 SHUTTER]   [🔄]    │  ← 컨트롤
│  Flash                  Flip    │
└─────────────────────────────────┘
│         [🎞️ Gallery]           │  ← 하단 탭
└─────────────────────────────────┘
```

### 6.3 디자인 토큰
```swift
// DesignTokens.swift
enum FilmLabColors {
    static let primary = Color("OrangeFilm")       // #FF6B35
    static let secondary = Color("CreamWhite")    // #F5F1E6
    static let background = Color("DarkRoom")     // #1A1A1A
    static let surface = Color("FilmBrown")       // #3D3529
    static let text = Color("LightCream")         // #EDE8DB
    static let accent = Color("KodakYellow")      // #FFD93D
}

enum FilmLabTypography {
    static let display = Font.custom("Courier", size: 32).weight(.bold)
    static let title = Font.custom("Courier", size: 24).weight(.semibold)
    static let body = Font.custom("Courier", size: 16)
    static let caption = Font.custom("Courier", size: 12)
    static let counter = Font.custom("DSEG7Classic", size: 48)  // 7세그먼트
}

enum FilmLabSpacing {
    static let xs: CGFloat = 4
    static let sm: CGFloat = 8
    static let md: CGFloat = 16
    static let lg: CGFloat = 24
    static let xl: CGFloat = 32
}
```

---

## 7. 필름 프리셋 시스템

### 7.1 기본 프리셋 (MVP)
```json
{
  "presets": [
    {
      "id": "kodak_gold_200",
      "name": "Kodak Gold 200",
      "category": "color",
      "isPremium": false,
      "params": {
        "temperature": 0.15,
        "tint": 0.05,
        "saturation": 1.1,
        "contrast": 1.05,
        "grain": { "intensity": 0.3, "size": 0.8 },
        "vignette": { "intensity": 0.2, "radius": 0.7 }
      }
    },
    {
      "id": "kodak_portra_400",
      "name": "Kodak Portra 400",
      "category": "color",
      "isPremium": false,
      "params": {
        "temperature": 0.08,
        "saturation": 0.95,
        "contrast": 0.98,
        "highlights": -0.1,
        "shadows": 0.15
      }
    },
    {
      "id": "fuji_superia_400",
      "name": "Fuji Superia 400",
      "category": "color",
      "isPremium": false,
      "params": {
        "temperature": -0.05,
        "saturation": 1.15,
        "contrast": 1.08,
        "vibrance": 1.1
      }
    },
    {
      "id": "ilford_hp5",
      "name": "Ilford HP5 Plus",
      "category": "bw",
      "isPremium": false,
      "params": {
        "saturation": 0,
        "contrast": 1.2,
        "grain": { "intensity": 0.5, "size": 1.0 }
      }
    },
    {
      "id": "cinestill_800t",
      "name": "Cinestill 800T",
      "category": "cinema",
      "isPremium": false,
      "params": {
        "temperature": -0.2,
        "tint": 0.1,
        "halation": 0.3,
        "contrast": 1.1
      }
    }
  ]
}
```

### 7.2 결함 생성 시스템
```swift
// DefectGenerator.swift
class DefectGenerator {
    
    func generateDefects(for roll: FilmRoll) -> [[DefectType]] {
        var photoDefects: [[DefectType]] = []
        
        for i in 0..<roll.currentCount {
            var defects: [DefectType] = []
            
            // 기본 그레인은 항상
            defects.append(.grain)
            
            // 비네팅 80% 확률
            if Double.random(in: 0...1) < 0.8 {
                defects.append(.vignette)
            }
            
            // 광선누출 - 롤 시작/끝 부분에 높은 확률
            let lightLeakProb = (i < 3 || i > roll.currentCount - 3) ? 0.4 : 0.1
            if Double.random(in: 0...1) < lightLeakProb {
                defects.append(.lightLeak)
            }
            
            // 먼지 15% 확률
            if Double.random(in: 0...1) < 0.15 {
                defects.append(.dust)
            }
            
            // 스크래치 5% 확률
            if Double.random(in: 0...1) < 0.05 {
                defects.append(.scratch)
            }
            
            photoDefects.append(defects)
        }
        
        return photoDefects
    }
}
```

---

## 8. 현상 시스템

### 8.1 현상 플로우
```
[촬영 완료] ──▶ [현상소 선택] ──▶ [현상 시작]
                    │                   │
                    ▼                   ▼
              ┌─────────┐        ┌─────────────┐
              │동네현상소│        │ 타이머 시작  │
              │(3시간)  │        │ 노티 예약   │
              └─────────┘        └──────┬──────┘
              ┌─────────┐               │
              │프로 랩  │               ▼
              │(1시간)  │        ┌─────────────┐
              └─────────┘        │   대기 중    │
              ┌─────────┐        │ (앱 종료 OK) │
              │빈티지   │        └──────┬──────┘
              │(24시간) │               │
              └─────────┘               ▼
                              ┌─────────────────┐
                              │  노티 수신 or   │
                              │  앱 재진입      │
                              └────────┬────────┘
                                       │
                                       ▼
                              ┌─────────────────┐
                              │ 현상 완료 처리   │
                              │ (필터+결함 적용) │
                              └────────┬────────┘
                                       │
                                       ▼
                              ┌─────────────────┐
                              │   갤러리 표시    │
                              └─────────────────┘
```

### 8.2 현상소 타입
```swift
enum DevelopmentLab: CaseIterable {
    case neighborhood  // 동네 현상소
    case professional  // 프로 랩
    case vintage       // 오래된 현상소
    
    var duration: TimeInterval {
        switch self {
        case .neighborhood: return 3 * 60 * 60      // 3시간
        case .professional: return 1 * 60 * 60      // 1시간
        case .vintage: return 24 * 60 * 60          // 24시간
        }
    }
    
    var defectMultiplier: Double {
        switch self {
        case .neighborhood: return 1.0   // 기본
        case .professional: return 0.5   // 결함 적음
        case .vintage: return 2.0        // 결함 많음
        }
    }
    
    var colorAccuracy: Double {
        switch self {
        case .neighborhood: return 0.9
        case .professional: return 1.0
        case .vintage: return 0.7
        }
    }
}
```

---

## 9. API 설계

### 9.1 로컬 Repository Interface
```swift
protocol FilmRollRepository {
    func getAllRolls() async -> [FilmRoll]
    func getRoll(by id: UUID) async -> FilmRoll?
    func getCurrentRoll() async -> FilmRoll?
    func save(_ roll: FilmRoll) async throws
    func delete(_ roll: FilmRoll) async throws
}

protocol PhotoRepository {
    func getPhotos(for rollId: UUID) async -> [Photo]
    func save(_ photo: Photo) async throws
    func delete(_ photo: Photo) async throws
    func getPhotoData(at path: String) async -> Data?
}

protocol PresetRepository {
    func getAllPresets() -> [FilmPreset]
    func getPreset(by id: String) -> FilmPreset?
    func getUnlockedPresets() -> [FilmPreset]
}
```

### 9.2 Firebase Functions (현상 푸시)
```javascript
// functions/index.js
exports.scheduleDevelopmentNotification = functions.firestore
  .document('users/{userId}/rolls/{rollId}')
  .onUpdate(async (change, context) => {
    const newData = change.after.data();
    
    if (newData.status === 'developing') {
      const developTime = newData.developDuration;
      const fcmToken = await getUserFCMToken(context.params.userId);
      
      // 지연 메시지 예약
      await admin.messaging().send({
        token: fcmToken,
        notification: {
          title: '🎞️ 현상 완료!',
          body: '필름이 현상되었습니다. 확인해보세요!'
        },
        android: {
          ttl: developTime * 1000
        },
        apns: {
          headers: {
            'apns-expiration': Math.floor(Date.now()/1000 + developTime).toString()
          }
        }
      });
    }
  });
```

---

## 10. 파일 구조

### 10.1 iOS 프로젝트 구조
```
FilmLab/
├── App/
│   ├── FilmLabApp.swift
│   ├── AppDelegate.swift
│   └── SceneDelegate.swift
│
├── Core/
│   ├── DI/
│   │   └── Container.swift
│   ├── Extensions/
│   │   ├── CIImage+Extensions.swift
│   │   └── Date+Extensions.swift
│   └── Utils/
│       ├── HapticManager.swift
│       └── SoundManager.swift
│
├── Domain/
│   ├── Entities/
│   │   ├── FilmRoll.swift
│   │   ├── Photo.swift
│   │   └── FilmPreset.swift
│   ├── Repositories/
│   │   ├── FilmRollRepository.swift
│   │   └── PhotoRepository.swift
│   └── UseCases/
│       ├── CapturePhotoUseCase.swift
│       ├── StartDevelopmentUseCase.swift
│       └── GetRollsUseCase.swift
│
├── Data/
│   ├── Local/
│   │   ├── CoreData/
│   │   │   ├── FilmLabModel.xcdatamodeld
│   │   │   └── PersistenceController.swift
│   │   └── FileStorage/
│   │       └── PhotoFileManager.swift
│   ├── Remote/
│   │   └── FirebaseService.swift
│   └── Repositories/
│       ├── FilmRollRepositoryImpl.swift
│       └── PhotoRepositoryImpl.swift
│
├── Presentation/
│   ├── Camera/
│   │   ├── CameraView.swift
│   │   ├── CameraViewModel.swift
│   │   ├── ViewfinderView.swift
│   │   └── Components/
│   │       ├── ShutterButton.swift
│   │       ├── FilmCounter.swift
│   │       └── FilmSelector.swift
│   │
│   ├── Gallery/
│   │   ├── GalleryView.swift
│   │   ├── GalleryViewModel.swift
│   │   ├── RollCardView.swift
│   │   └── FilmStripView.swift
│   │
│   ├── Development/
│   │   ├── DevelopingView.swift
│   │   └── LabSelectionView.swift
│   │
│   ├── Settings/
│   │   ├── SettingsView.swift
│   │   └── SubscriptionView.swift
│   │
│   └── Common/
│       ├── DesignTokens.swift
│       └── Components/
│           └── VintageButton.swift
│
├── Services/
│   ├── Camera/
│   │   ├── CameraManager.swift
│   │   └── FilterEngine.swift
│   ├── Development/
│   │   ├── DevelopmentManager.swift
│   │   └── DefectGenerator.swift
│   ├── Notification/
│   │   └── NotificationManager.swift
│   └── IAP/
│       └── StoreManager.swift
│
└── Resources/
    ├── Assets.xcassets
    ├── Presets/
    │   └── film_presets.json
    ├── Sounds/
    │   ├── shutter.wav
    │   └── film_advance.wav
    ├── LUTs/
    │   └── (3D LUT files)
    └── Localizable.strings
```

---

## 11. 개발 일정

### 11.1 전체 타임라인 (12주)
```
Week 1-2:   🏗️ 프로젝트 셋업 & 아키텍처
Week 3-4:   📷 카메라 모듈 개발
Week 5-6:   🎨 필터 엔진 & 프리셋
Week 7-8:   ⏳ 현상 시스템 & 갤러리
Week 9-10:  💰 IAP & 구독 시스템
Week 11:    🐛 QA & 버그 수정
Week 12:    🚀 런칭 준비 & 제출
```

### 11.2 주차별 상세 태스크

#### Week 1-2: 프로젝트 셋업
```
□ Xcode/Android Studio 프로젝트 생성
□ Clean Architecture 폴더 구조
□ Core Data / Room 스키마 정의
□ Firebase 프로젝트 연동
□ CI/CD 파이프라인 (Fastlane)
□ 기본 네비게이션 구조
□ 디자인 토큰 정의
```

#### Week 3-4: 카메라 모듈
```
□ AVFoundation / CameraX 설정
□ 카메라 권한 처리
□ 프리뷰 레이어 구현
□ 촬영 기능 구현
□ 전/후면 전환
□ 플래시 on/off
□ 타이머 기능
□ 셔터 사운드 & 햅틱
```

#### Week 5-6: 필터 엔진
```
□ Core Image / GPUImage 설정
□ 실시간 필터 적용 (Metal)
□ 필름 프리셋 5종 구현
□ 결함 효과 구현 (grain, leak, dust)
□ LUT 적용 시스템
□ 프리셋 JSON 파싱
□ 필터 미리보기
```

#### Week 7-8: 현상 & 갤러리
```
□ 필름 롤 관리 시스템
□ 24장 촬영 제한 로직
□ 현상 타이머 시스템
□ 로컬 노티피케이션
□ 현상 완료 처리
□ 갤러리 UI (롤 목록)
□ 필름 스트립 뷰
□ 사진 상세 보기
□ 공유 기능
```

#### Week 9-10: 수익화
```
□ StoreKit 2 / Google Billing 연동
□ 구독 상품 등록
□ 구독 상태 관리
□ 광고 SDK 연동 (AdMob)
□ 보상형 광고 (즉시 현상)
□ 필름 팩 IAP
□ Paywall UI
□ 복원 구매
```

#### Week 11: QA
```
□ 단위 테스트 작성
□ UI 테스트 작성
□ 메모리 누수 체크
□ 배터리 소모 최적화
□ 다양한 기기 테스트
□ 크래시 수정
□ 성능 최적화
```

#### Week 12: 런칭
```
□ 앱스토어 메타데이터
□ 스크린샷 제작
□ 프리뷰 영상
□ 개인정보 처리방침
□ 앱 심사 제출
□ 런칭 마케팅 준비
```

---

## 12. 테스트 계획

### 12.1 단위 테스트
```swift
// FilterEngineTests.swift
class FilterEngineTests: XCTestCase {
    var sut: FilmFilterEngine!
    
    func test_applyKodakGold_increasesWarmth() {
        let input = CIImage(color: .white)
        let preset = PresetRepository().getPreset(by: "kodak_gold_200")!
        
        let output = sut.applyFilmLook(to: input, preset: preset)
        
        // 색온도 증가 확인
        XCTAssertTrue(output.averageTemperature > input.averageTemperature)
    }
    
    func test_defectGenerator_respectsProbability() {
        let generator = DefectGenerator()
        let roll = FilmRoll(filmPresetId: "kodak_gold_200", maxPhotos: 24)
        
        // 1000번 실행하여 확률 검증
        var lightLeakCount = 0
        for _ in 0..<1000 {
            let defects = generator.generateDefects(for: roll)
            if defects[0].contains(.lightLeak) { lightLeakCount += 1 }
        }
        
        // 첫 프레임 광선누출 확률 ~40%
        XCTAssertTrue((350...450).contains(lightLeakCount))
    }
}
```

### 12.2 UI 테스트
```swift
// CameraUITests.swift
class CameraUITests: XCTestCase {
    func test_capturePhoto_incrementsCounter() {
        let app = XCUIApplication()
        app.launch()
        
        let counterBefore = app.staticTexts["filmCounter"].label
        
        app.buttons["shutterButton"].tap()
        
        let counterAfter = app.staticTexts["filmCounter"].label
        XCTAssertEqual(Int(counterAfter)!, Int(counterBefore)! + 1)
    }
    
    func test_fullRoll_showsDevelopButton() {
        // 24장 촬영 후 현상 버튼 표시 확인
    }
}
```

### 12.3 QA 체크리스트
```
카메라
□ 전면/후면 전환 정상
□ 플래시 동작 정상
□ 저조도 촬영 품질
□ HDR 동작 확인
□ 메모리 경고 시 동작
□ 백그라운드 전환 후 복귀

필터
□ 모든 프리셋 적용 확인
□ 실시간 프리뷰 성능 (60fps)
□ 고해상도 이미지 처리
□ 결함 효과 자연스러움

현상
□ 백그라운드에서 타이머 동작
□ 앱 종료 후 재시작 시 상태 유지
□ 노티피케이션 정상 수신
□ 시간대 변경 시 동작

갤러리
□ 대용량 롤 로딩 성능
□ 스크롤 성능 (60fps)
□ 이미지 캐싱 동작
□ 공유 기능 모든 앱

결제
□ 구독 구매 플로우
□ 복원 구매
□ 구독 취소 후 상태
□ 네트워크 에러 처리
```

---

## 13. 출시 체크리스트

### 13.1 앱스토어 준비
```
메타데이터
□ 앱 이름: 필름랩 - 빈티지 필름 카메라
□ 부제: 현상까지 기다리는 아날로그 감성
□ 키워드: 필름,카메라,빈티지,레트로,kodak,아날로그
□ 카테고리: Photo & Video
□ 연령 등급: 4+

에셋
□ 앱 아이콘 (1024x1024)
□ 스크린샷 6.7인치 (1290x2796) x 6장
□ 스크린샷 5.5인치 (1242x2208) x 6장
□ 앱 프리뷰 영상 (선택)

문서
□ 개인정보 처리방침 URL
□ 지원 URL
□ 마케팅 URL (선택)
□ EULA (선택)
```

### 13.2 런칭 마케팅
```
□ 프레스킷 준비
□ 인플루언서 시딩 (20명)
□ ProductHunt 등록
□ 앱스토어 피처링 신청
□ SNS 계정 개설 (@filmlab_app)
□ 런칭 트레일러 영상
```

---

*끝. 이 설계서를 기반으로 개발을 시작하세요!* 🎞️
