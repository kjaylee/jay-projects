# 🟢 슬라임 서바이버 (Slime Survivor) — 상세 개발 설계서

**버전**: 1.0  
**작성일**: 2026년 1월 27일  
**예상 개발기간**: 20주 (5개월)

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택](#2-기술-스택)
3. [게임 아키텍처](#3-게임-아키텍처)
4. [데이터베이스 설계](#4-데이터베이스-설계)
5. [핵심 게임 시스템](#5-핵심-게임-시스템)
6. [슬라임 시스템](#6-슬라임-시스템)
7. [전투 시스템](#7-전투-시스템)
8. [가챠 & 합성 시스템](#8-가챠--합성-시스템)
9. [경제 시스템](#9-경제-시스템)
10. [UI/UX 설계](#10-uiux-설계)
11. [서버 아키텍처](#11-서버-아키텍처)
12. [개발 일정](#12-개발-일정)
13. [라이브 서비스 계획](#13-라이브-서비스-계획)

---

## 1. 프로젝트 개요

### 1.1 제품 정의
| 항목 | 내용 |
|------|------|
| 게임 이름 | 슬라임 서바이버 |
| 장르 | 럭 배틀러 + 로그라이크 + 수집형 |
| 플랫폼 | iOS / Android |
| 타겟 | 캐주얼~미드코어, 15-35세 |
| 핵심 가치 | "수집, 합성, 도박의 재미" |

### 1.2 핵심 게임 루프
```
┌──────────────────────────────────────────────────┐
│                    CORE LOOP                      │
│                                                   │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐     │
│   │ 슬라임  │───▶│  전투   │───▶│  보상   │     │
│   │  배치   │    │ (자동)  │    │  획득   │     │
│   └────┬────┘    └─────────┘    └────┬────┘     │
│        │                              │          │
│        │    ┌─────────────────┐      │          │
│        │    │                 │      │          │
│        └────│  슬라임 합성    │◀─────┘          │
│             │  (도박 요소)    │                  │
│             └─────────────────┘                  │
└──────────────────────────────────────────────────┘
```

### 1.3 MVP 기능 범위
```
✅ MVP 포함
- 슬라임 30종 (5등급)
- 3x3 배치 전투
- 10스테이지 (1챕터)
- 합성 시스템
- 기본 가챠
- 영구 업그레이드
- 광고 수익화

❌ v2.0 이후
- 길드 시스템
- PvP 아레나
- 시즌 패스
- 월드 보스
- 추가 챕터
```

### 1.4 성공 지표 (KPI)
| 지표 | 목표 |
|------|------|
| Day 1 Retention | 40%+ |
| Day 7 Retention | 15%+ |
| Day 30 Retention | 5%+ |
| ARPDAU | $0.10+ |
| 세션 길이 | 8분+ |
| 일일 세션 수 | 4회+ |

---

## 2. 기술 스택

### 2.1 게임 엔진
```yaml
Engine: Unity 2023.3 LTS
  - Rendering: URP (Universal Render Pipeline)
  - UI: UI Toolkit + TextMeshPro
  - Audio: FMOD (선택) or Unity Audio
  - Animation: DOTween + Spine (2D 애니메이션)

Language: C# (.NET Standard 2.1)
Architecture: 
  - ECS-lite (Entities for combat)
  - MVVM for UI
  - Service Locator for DI
```

### 2.2 백엔드
```yaml
Primary: PlayFab (Microsoft)
  - 장점: 게임 특화, 리더보드, 매치메이킹 내장
  - 용도: 유저 데이터, 경제, 랭킹

Alternative: Firebase
  - Auth: 소셜 로그인
  - Firestore: 실시간 데이터
  - Analytics: 행동 분석

Server Logic: Azure Functions / Cloud Functions
  - 가챠 검증
  - 치트 방지
  - 보상 지급
```

### 2.3 주요 패키지
```yaml
Unity Packages:
  - Addressables (에셋 관리)
  - Localization (다국어)
  - In-App Purchasing (IAP)
  - Mobile Notifications
  - Unity Ads / AdMob Mediation

Third Party:
  - DOTween Pro (애니메이션)
  - Spine Runtime (2D 스켈레탈)
  - Odin Inspector (에디터)
  - UniTask (비동기)
  - MessagePipe (이벤트)
```

---

## 3. 게임 아키텍처

### 3.1 전체 구조
```
┌─────────────────────────────────────────────────────┐
│                   Presentation Layer                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │  Battle  │ │  Slime   │ │  Gacha   │ │  Shop  │ │
│  │   View   │ │ Manage   │ │   View   │ │  View  │ │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └───┬────┘ │
└───────┼────────────┼────────────┼───────────┼──────┘
        │            │            │           │
        ▼            ▼            ▼           ▼
┌─────────────────────────────────────────────────────┐
│                   ViewModel Layer                    │
│  ┌──────────────────────────────────────────────┐   │
│  │  BattleVM │ SlimeVM │ GachaVM │ ShopVM │ ... │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│                    Domain Layer                      │
│  ┌────────────────────────────────────────────────┐ │
│  │                   UseCases                      │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐          │ │
│  │  │ Battle  │ │  Merge  │ │  Gacha  │  ...     │ │
│  │  │ UseCase │ │ UseCase │ │ UseCase │          │ │
│  │  └─────────┘ └─────────┘ └─────────┘          │ │
│  └────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────┐ │
│  │                   Entities                      │ │
│  │  Slime │ Battle │ Player │ Stage │ Item │ ... │ │
│  └────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────┐
│                     Data Layer                       │
│  ┌────────────┐  ┌────────────┐  ┌──────────────┐  │
│  │   Local    │  │   Remote   │  │  Static Data │  │
│  │ (PlayerPfs)│  │ (PlayFab)  │  │  (SO/JSON)   │  │
│  └────────────┘  └────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 3.2 씬 구조
```
Scenes/
├── Bootstrap.unity        # 초기화, 로그인
├── Main.unity            # 메인 로비, 영구 씬
├── Battle.unity          # 전투 씬 (Additive)
├── Gacha.unity           # 가챠 연출 (Additive)
└── Loading.unity         # 로딩 화면
```

### 3.3 게임 상태 머신
```csharp
public enum GameState
{
    Initializing,    // 앱 시작, 초기화
    Login,           // 로그인 처리
    Lobby,           // 메인 로비
    PreBattle,       // 전투 준비 (배치)
    Battle,          // 전투 중
    BattleResult,    // 전투 결과
    Gacha,           // 가챠 연출
    Loading          // 씬 전환
}

public class GameStateManager : MonoBehaviour
{
    public ReactiveProperty<GameState> CurrentState { get; }
    
    public async UniTask ChangeState(GameState newState)
    {
        await OnStateExit(CurrentState.Value);
        CurrentState.Value = newState;
        await OnStateEnter(newState);
    }
}
```

---

## 4. 데이터베이스 설계

### 4.1 클라이언트 로컬 데이터 (PlayerPrefs/JSON)
```csharp
[Serializable]
public class LocalPlayerData
{
    public string playerId;
    public int tutorialProgress;
    public float musicVolume;
    public float sfxVolume;
    public string language;
    public DateTime lastPlayTime;
    
    // 오프라인 진행용 캐시
    public List<OwnedSlime> slimeCache;
    public int goldCache;
    public int gemCache;
}
```

### 4.2 서버 데이터 (PlayFab/Firestore)
```csharp
// Player Profile
{
    "PlayFabId": "ABC123",
    "DisplayName": "Player#1234",
    "Created": "2026-01-27T00:00:00Z",
    "LastLogin": "2026-01-27T12:00:00Z",
    "Statistics": {
        "HighestStage": 45,
        "TotalBattles": 1000,
        "TotalMerges": 500,
        "GachaCount": 100
    }
}

// Player Inventory (Virtual Currency)
{
    "GD": 50000,    // Gold
    "GM": 500,      // Gems
    "EN": 100       // Energy
}

// Player Data (Custom)
{
    "OwnedSlimes": [
        {"id": "slime_001", "slimeDefId": "fire_jelly", "level": 5, "exp": 120},
        {"id": "slime_002", "slimeDefId": "aqua_blob", "level": 3, "exp": 50}
    ],
    "Formation": {
        "slots": [
            {"position": 0, "slimeId": "slime_001"},
            {"position": 4, "slimeId": "slime_002"}
        ]
    },
    "Upgrades": {
        "attackBonus": 5,
        "healthBonus": 3,
        "goldBonus": 2
    },
    "Progress": {
        "currentChapter": 1,
        "currentStage": 7,
        "clearedStages": [1,2,3,4,5,6]
    }
}
```

### 4.3 정적 데이터 (ScriptableObject)
```csharp
// SlimeDefinition.cs
[CreateAssetMenu]
public class SlimeDefinition : ScriptableObject
{
    [Header("Basic Info")]
    public string slimeId;
    public string displayName;
    public Rarity rarity;
    public Element element;
    public Sprite icon;
    public RuntimeAnimatorController animator;
    
    [Header("Base Stats")]
    public int baseHp;
    public int baseAttack;
    public int baseDefense;
    public float attackSpeed;
    public float attackRange;
    
    [Header("Growth")]
    public float hpPerLevel;
    public float attackPerLevel;
    public float defensePerLevel;
    
    [Header("Skills")]
    public SkillDefinition normalAttack;
    public SkillDefinition specialSkill;
    
    [Header("Merge")]
    public SlimeDefinition[] mergeResults;  // 합성 결과 후보
    public float[] mergeResultWeights;      // 각 결과 확률
}

// StageDefinition.cs
[CreateAssetMenu]
public class StageDefinition : ScriptableObject
{
    public int stageId;
    public int chapterId;
    public string stageName;
    
    [Header("Waves")]
    public WaveData[] waves;
    
    [Header("Rewards")]
    public RewardData clearReward;
    public RewardData firstClearReward;
    
    [Header("Requirements")]
    public int recommendedPower;
    public int energyCost;
}

[Serializable]
public class WaveData
{
    public EnemySpawn[] enemies;
    public float spawnDelay;
}
```

---

## 5. 핵심 게임 시스템

### 5.1 런(Run) 시스템
```csharp
public class RunManager
{
    private RunState _currentRun;
    
    public async UniTask StartRun(int stageId)
    {
        _currentRun = new RunState
        {
            StageId = stageId,
            CurrentWave = 0,
            Formation = _playerData.Formation.Clone(),
            StartTime = DateTime.UtcNow
        };
        
        await _sceneManager.LoadBattleScene();
        await _battleManager.InitializeBattle(_currentRun);
    }
    
    public async UniTask<RunResult> EndRun(bool victory)
    {
        var result = new RunResult
        {
            Victory = victory,
            WavesCleared = _currentRun.CurrentWave,
            Duration = DateTime.UtcNow - _currentRun.StartTime,
            Rewards = CalculateRewards(victory)
        };
        
        await _rewardManager.GrantRewards(result.Rewards);
        await _progressManager.UpdateProgress(result);
        
        return result;
    }
}
```

### 5.2 스테이지 진행
```
[챕터 1 구성]
Stage 1-3:   튜토리얼 (쉬움)
Stage 4-6:   기본 전투 (보통)
Stage 7-9:   챌린지 (어려움)
Stage 10:    챕터 보스

[난이도 스케일링]
Enemy HP = BaseHP * (1 + 0.1 * StageNum)
Enemy ATK = BaseATK * (1 + 0.08 * StageNum)
```

---

## 6. 슬라임 시스템

### 6.1 등급 & 확률
```csharp
public enum Rarity
{
    Common,      // 흰색  - 50%
    Uncommon,    // 초록  - 30%
    Rare,        // 파랑  - 15%
    Epic,        // 보라  - 4%
    Legendary    // 금색  - 1%
}

public static class GachaRates
{
    public static readonly Dictionary<Rarity, float> Rates = new()
    {
        { Rarity.Common, 0.50f },
        { Rarity.Uncommon, 0.30f },
        { Rarity.Rare, 0.15f },
        { Rarity.Epic, 0.04f },
        { Rarity.Legendary, 0.01f }
    };
    
    // 천장 시스템
    public const int PITY_EPIC = 50;       // 50연차 에픽 보장
    public const int PITY_LEGENDARY = 100;  // 100연차 레전더리 보장
}
```

### 6.2 속성 시스템
```csharp
public enum Element
{
    Fire,    // 🔴 불 > 풀
    Water,   // 🔵 물 > 불
    Grass,   // 🟢 풀 > 물
    Light,   // 🟡 빛 ↔ 어둠
    Dark     // 🟣 어둠 ↔ 빛
}

public static class ElementSystem
{
    // 상성 데미지 배율
    public const float ADVANTAGE = 1.3f;
    public const float DISADVANTAGE = 0.7f;
    public const float NEUTRAL = 1.0f;
    
    public static float GetMultiplier(Element attacker, Element defender)
    {
        return (attacker, defender) switch
        {
            (Element.Fire, Element.Grass) => ADVANTAGE,
            (Element.Fire, Element.Water) => DISADVANTAGE,
            (Element.Water, Element.Fire) => ADVANTAGE,
            (Element.Water, Element.Grass) => DISADVANTAGE,
            (Element.Grass, Element.Water) => ADVANTAGE,
            (Element.Grass, Element.Fire) => DISADVANTAGE,
            (Element.Light, Element.Dark) => ADVANTAGE,
            (Element.Dark, Element.Light) => ADVANTAGE,
            _ => NEUTRAL
        };
    }
}
```

### 6.3 슬라임 목록 (MVP 30종)
```yaml
# Common (15종)
fire_jelly:      { element: Fire, baseAtk: 10, baseHp: 100 }
aqua_blob:       { element: Water, baseAtk: 8, baseHp: 120 }
leaf_slime:      { element: Grass, baseAtk: 9, baseHp: 110 }
light_pudding:   { element: Light, baseAtk: 11, baseHp: 90 }
shadow_goo:      { element: Dark, baseAtk: 12, baseHp: 85 }
# ... 10종 더

# Uncommon (8종)
flame_sphere:    { element: Fire, baseAtk: 18, baseHp: 160 }
ocean_drop:      { element: Water, baseAtk: 15, baseHp: 200 }
forest_blob:     { element: Grass, baseAtk: 16, baseHp: 180 }
# ... 5종 더

# Rare (4종)
inferno_slime:   { element: Fire, baseAtk: 30, baseHp: 250 }
tsunami_jelly:   { element: Water, baseAtk: 25, baseHp: 320 }
# ... 2종 더

# Epic (2종)
phoenix_goo:     { element: Fire, baseAtk: 50, baseHp: 400 }
kraken_blob:     { element: Water, baseAtk: 45, baseHp: 500 }

# Legendary (1종)
rainbow_slime:   { element: null, baseAtk: 80, baseHp: 600 }  # 전 속성
```

---

## 7. 전투 시스템

### 7.1 전투 그리드
```
[3x3 배치 그리드]
┌─────┬─────┬─────┐
│ 0,0 │ 1,0 │ 2,0 │  ← 후열 (방어↑, 원거리)
├─────┼─────┼─────┤
│ 0,1 │ 1,1 │ 2,1 │  ← 중열 (스킬↑)
├─────┼─────┼─────┤
│ 0,2 │ 1,2 │ 2,2 │  ← 전열 (공격↑, 근접)
└─────┴─────┴─────┘

[위치 보너스]
전열: ATK +20%, DEF -10%
중열: Skill Damage +15%
후열: DEF +20%, ATK -10%
```

### 7.2 전투 플로우
```csharp
public class BattleManager : MonoBehaviour
{
    private BattleState _state;
    private List<BattleSlime> _playerSlimes;
    private List<BattleEnemy> _enemies;
    
    public async UniTask RunBattle()
    {
        _state = BattleState.Running;
        
        while (_state == BattleState.Running)
        {
            // 1. 슬라임 행동 (자동)
            foreach (var slime in _playerSlimes.Where(s => s.IsAlive))
            {
                if (slime.CanAttack())
                {
                    var target = SelectTarget(slime);
                    await slime.Attack(target);
                }
            }
            
            // 2. 적 행동
            foreach (var enemy in _enemies.Where(e => e.IsAlive))
            {
                if (enemy.CanAttack())
                {
                    var target = SelectPlayerTarget(enemy);
                    await enemy.Attack(target);
                }
            }
            
            // 3. 승패 체크
            CheckBattleEnd();
            
            await UniTask.Yield();
        }
    }
    
    private void CheckBattleEnd()
    {
        if (_enemies.All(e => !e.IsAlive))
        {
            _state = BattleState.Victory;
        }
        else if (_playerSlimes.All(s => !s.IsAlive))
        {
            _state = BattleState.Defeat;
        }
    }
}
```

### 7.3 데미지 계산
```csharp
public class DamageCalculator
{
    public static DamageResult Calculate(BattleUnit attacker, BattleUnit defender, SkillDefinition skill)
    {
        // 기본 데미지
        float baseDamage = attacker.Attack * skill.damageMultiplier;
        
        // 속성 보너스
        float elementMult = ElementSystem.GetMultiplier(attacker.Element, defender.Element);
        
        // 치명타 체크
        bool isCritical = Random.value < attacker.CritRate;
        float critMult = isCritical ? attacker.CritDamage : 1f;
        
        // 방어력 적용
        float defense = defender.Defense;
        float reduction = defense / (defense + 100);  // 방어력 100 = 50% 감소
        
        // 최종 데미지
        float finalDamage = baseDamage * elementMult * critMult * (1 - reduction);
        
        // 랜덤 변동 (±10%)
        finalDamage *= Random.Range(0.9f, 1.1f);
        
        return new DamageResult
        {
            Damage = Mathf.RoundToInt(finalDamage),
            IsCritical = isCritical,
            ElementAdvantage = elementMult > 1f,
            ElementDisadvantage = elementMult < 1f
        };
    }
}
```

---

## 8. 가챠 & 합성 시스템

### 8.1 가챠 시스템
```csharp
public class GachaSystem
{
    private readonly IRandomService _random;
    private readonly IPlayerDataService _playerData;
    
    public async UniTask<GachaResult> Pull(GachaBanner banner, int count)
    {
        var results = new List<SlimeDefinition>();
        
        for (int i = 0; i < count; i++)
        {
            // 천장 체크
            int pityCount = _playerData.GetPityCount(banner.Id);
            
            SlimeDefinition slime;
            if (pityCount >= GachaRates.PITY_LEGENDARY)
            {
                slime = GetGuaranteedLegendary(banner);
                _playerData.ResetPity(banner.Id);
            }
            else if (pityCount >= GachaRates.PITY_EPIC && pityCount % GachaRates.PITY_EPIC == 0)
            {
                slime = GetGuaranteedEpic(banner);
            }
            else
            {
                slime = RollNormal(banner);
            }
            
            results.Add(slime);
            _playerData.IncrementPity(banner.Id);
        }
        
        // 서버 검증
        var verified = await _serverService.VerifyGacha(banner.Id, count, results);
        
        // 인벤토리에 추가
        await _inventoryService.AddSlimes(verified);
        
        return new GachaResult { Slimes = verified };
    }
    
    private SlimeDefinition RollNormal(GachaBanner banner)
    {
        float roll = _random.Range(0f, 1f);
        float cumulative = 0f;
        
        foreach (var (rarity, rate) in GachaRates.Rates)
        {
            cumulative += rate * banner.RateModifiers.GetValueOrDefault(rarity, 1f);
            if (roll <= cumulative)
            {
                return GetRandomSlimeOfRarity(rarity, banner);
            }
        }
        
        return GetRandomSlimeOfRarity(Rarity.Common, banner);
    }
}
```

### 8.2 합성 시스템
```csharp
public class MergeSystem
{
    // 동일 슬라임 3개 → 상위 등급 1개
    public MergeResult Merge(OwnedSlime slime1, OwnedSlime slime2, OwnedSlime slime3)
    {
        // 검증
        if (!CanMerge(slime1, slime2, slime3))
            throw new InvalidMergeException();
        
        var definition = _database.GetSlimeDefinition(slime1.SlimeDefId);
        
        // 결과 결정 (가중치 기반 랜덤)
        var resultDef = SelectMergeResult(definition);
        
        // 새 슬라임 생성
        var newSlime = new OwnedSlime
        {
            Id = Guid.NewGuid().ToString(),
            SlimeDefId = resultDef.slimeId,
            Level = 1,
            Exp = 0,
            ObtainedAt = DateTime.UtcNow
        };
        
        // 인벤토리 업데이트
        _inventory.Remove(slime1, slime2, slime3);
        _inventory.Add(newSlime);
        
        return new MergeResult
        {
            ConsumedSlimes = new[] { slime1, slime2, slime3 },
            ResultSlime = newSlime,
            IsRareResult = resultDef.rarity > definition.rarity
        };
    }
    
    private bool CanMerge(params OwnedSlime[] slimes)
    {
        // 3개 필요
        if (slimes.Length != 3) return false;
        
        // 동일 슬라임
        var defId = slimes[0].SlimeDefId;
        if (!slimes.All(s => s.SlimeDefId == defId)) return false;
        
        // 편성 중이 아닌지
        if (slimes.Any(s => _formation.Contains(s))) return false;
        
        return true;
    }
    
    private SlimeDefinition SelectMergeResult(SlimeDefinition source)
    {
        var candidates = source.mergeResults;
        var weights = source.mergeResultWeights;
        
        float total = weights.Sum();
        float roll = Random.Range(0f, total);
        float cumulative = 0f;
        
        for (int i = 0; i < candidates.Length; i++)
        {
            cumulative += weights[i];
            if (roll <= cumulative)
                return candidates[i];
        }
        
        return candidates[0];
    }
}
```

### 8.3 특수 레시피 합성
```csharp
// 특정 조합 → 특별한 슬라임
public static class SpecialRecipes
{
    public static readonly Dictionary<string[], string> Recipes = new()
    {
        // 파이어젤리 + 아쿠아블롭 + 리프슬라임 = 레인보우슬라임
        { new[] { "fire_jelly", "aqua_blob", "leaf_slime" }, "rainbow_slime" },
        
        // 라이트푸딩 + 섀도우구 + 섀도우구 = 이클립스슬라임
        { new[] { "light_pudding", "shadow_goo", "shadow_goo" }, "eclipse_slime" }
    };
    
    public static bool TryGetSpecialResult(OwnedSlime[] slimes, out string resultId)
    {
        var ids = slimes.Select(s => s.SlimeDefId).OrderBy(s => s).ToArray();
        
        foreach (var (recipe, result) in Recipes)
        {
            if (recipe.OrderBy(s => s).SequenceEqual(ids))
            {
                resultId = result;
                return true;
            }
        }
        
        resultId = null;
        return false;
    }
}
```

---

## 9. 경제 시스템

### 9.1 재화 종류
```csharp
public enum Currency
{
    Gold,      // 🪙 기본 재화 (전투, 일퀘)
    Gem,       // 💎 프리미엄 재화 (구매, 업적)
    Energy     // ⚡ 스태미나 (전투 소모)
}

public class EconomyConfig
{
    // 골드
    public const int STAGE_GOLD_BASE = 100;
    public const float STAGE_GOLD_MULTIPLIER = 1.1f;  // 스테이지당 10% 증가
    
    // 젬
    public const int DAILY_FREE_GEM = 50;
    public const int AD_WATCH_GEM = 10;
    public const int ACHIEVEMENT_GEM_SMALL = 20;
    public const int ACHIEVEMENT_GEM_LARGE = 100;
    
    // 에너지
    public const int MAX_ENERGY = 100;
    public const int ENERGY_REGEN_MINUTES = 5;  // 5분당 1 회복
    public const int STAGE_ENERGY_COST = 5;
    
    // 가챠 비용
    public const int GACHA_SINGLE_GEM = 100;
    public const int GACHA_TEN_GEM = 900;  // 10% 할인
}
```

### 9.2 IAP 상품 구성
```csharp
public static class IAPProducts
{
    // 젬 패키지
    public static readonly IAPProduct[] GemPacks = 
    {
        new("gem_small",   99,   100),   // $0.99 → 100젬
        new("gem_medium",  499,  550),   // $4.99 → 550젬 (10% 보너스)
        new("gem_large",   999,  1200),  // $9.99 → 1200젬 (20% 보너스)
        new("gem_huge",    4999, 6500),  // $49.99 → 6500젬 (30% 보너스)
        new("gem_mega",    9999, 14000)  // $99.99 → 14000젬 (40% 보너스)
    };
    
    // 특별 패키지
    public static readonly IAPProduct StarterPack = new("starter_pack", 299)
    {
        Gems = 500,
        Slimes = new[] { "rare_random", "rare_random", "rare_random" },
        Gold = 10000,
        LimitPerUser = 1
    };
    
    // 구독
    public static readonly IAPProduct MonthlyPass = new("monthly_pass", 499)
    {
        Type = ProductType.Subscription,
        Duration = 30,
        DailyGems = 100,
        NoAds = true,
        BonusGachaRate = 0.1f  // 10% 확률 증가
    };
}
```

### 9.3 광고 수익화
```csharp
public class AdManager
{
    // 보상형 광고 포인트
    public enum RewardedAdPlacement
    {
        DoubleStageReward,     // 스테이지 보상 2배
        FreeGacha,             // 무료 가챠 1회
        EnergyRefill,          // 에너지 50 충전
        ReviveBattle,          // 전투 이어하기
        SpeedUpMerge           // 합성 즉시 완료
    }
    
    // 일일 광고 한도
    public const int MAX_REWARDED_ADS_PER_DAY = 10;
    
    // 전면 광고 (비보상)
    public const int INTERSTITIAL_INTERVAL_BATTLES = 3;  // 3전투마다
}
```

---

## 10. UI/UX 설계

### 10.1 화면 구조
```
App
├── 🏠 LobbyScreen (Main)
│   ├── TopBar (Currency, Profile)
│   ├── CenterArea (Stage Select, News)
│   └── BottomNav (Battle, Slimes, Gacha, Shop)
│
├── ⚔️ BattleScreen
│   ├── FormationPanel (3x3 Grid)
│   ├── BattleField
│   ├── WaveIndicator
│   └── AutoButton, SpeedButton
│
├── 🟢 SlimeScreen
│   ├── SlimeList (Grid View)
│   ├── SlimeDetail (Stats, Skills)
│   ├── MergePanel
│   └── FormationEdit
│
├── 🎰 GachaScreen
│   ├── BannerCarousel
│   ├── PullButtons (1x, 10x)
│   ├── PityCounter
│   └── ResultAnimation
│
├── 🛒 ShopScreen
│   ├── GemPacks
│   ├── SpecialOffers
│   ├── MonthlyPass
│   └── FreeRewards (Ads)
│
└── ⚙️ SettingsScreen
    ├── Account
    ├── Sound
    ├── Language
    └── Support
```

### 10.2 메인 로비 레이아웃
```
┌─────────────────────────────────────┐
│  [👤]        🪙 50,000  💎 500      │  ← 상단바
├─────────────────────────────────────┤
│                                     │
│     ┌─────────────────────────┐     │
│     │                         │     │
│     │      STAGE 1-7          │     │  ← 메인 영역
│     │      [▶ START]          │     │
│     │                         │     │
│     └─────────────────────────┘     │
│                                     │
│   [📰 News]         [📋 Missions]  │  ← 서브 버튼
│                                     │
├─────────────────────────────────────┤
│  [⚔️]   [🟢]   [🎰]   [🛒]   [⚙️] │  ← 하단 네비게이션
│ Battle Slime  Gacha  Shop Setting  │
└─────────────────────────────────────┘
```

### 10.3 배틀 화면 레이아웃
```
┌─────────────────────────────────────┐
│  Wave 3/10        [⏸️] [▶▶ 2x]    │  ← 웨이브 / 컨트롤
├─────────────────────────────────────┤
│                                     │
│     👾  👾  👾                      │  ← 적 영역
│        👾  👾                       │
│                                     │
│  ═══════════════════════════════   │  ← 구분선
│                                     │
│     🟢  🟢  🟢                      │
│     🟢  🔵  🟢                      │  ← 아군 영역 (3x3)
│     🔴  🟢  🟣                      │
│                                     │
├─────────────────────────────────────┤
│  [AUTO: ON]              [SKILL]   │  ← 하단 컨트롤
└─────────────────────────────────────┘
```

---

## 11. 서버 아키텍처

### 11.1 PlayFab 설정
```javascript
// Cloud Script: 가챠 검증
handlers.verifyGacha = function(args, context) {
    const playerId = context.userId;
    const bannerId = args.bannerId;
    const count = args.count;
    const clientResults = args.results;
    
    // 서버에서 재계산
    const serverResults = performGacha(bannerId, count, playerId);
    
    // 결과 비교 (해시 검증)
    if (!verifyResults(clientResults, serverResults)) {
        return { error: "GACHA_VERIFICATION_FAILED" };
    }
    
    // 재화 차감
    const cost = count === 10 ? 900 : count * 100;
    deductCurrency(playerId, "GM", cost);
    
    // 인벤토리에 추가
    addToInventory(playerId, serverResults);
    
    // 천장 카운터 업데이트
    updatePityCounter(playerId, bannerId, count);
    
    return { success: true, results: serverResults };
};

// 일일 리셋
handlers.dailyReset = function(args, context) {
    const playerId = context.userId;
    
    // 일일 보상 지급
    grantCurrency(playerId, "GM", 50);  // 일일 젬
    
    // 광고 카운터 리셋
    resetAdCounter(playerId);
    
    // 무료 가챠 리셋
    resetFreeGacha(playerId);
    
    return { success: true };
};
```

### 11.2 치트 방지
```csharp
public class AntiCheatSystem
{
    // 클라이언트 → 서버 검증 필요 항목
    public static readonly string[] ServerVerifiedActions = 
    {
        "gacha_pull",
        "slime_merge",
        "currency_purchase",
        "stage_clear"
    };
    
    // 스테이지 클리어 검증
    public async UniTask<bool> VerifyStageClear(int stageId, float clearTime, int score)
    {
        // 최소 클리어 시간 체크 (너무 빠르면 의심)
        float minTime = GetMinimumClearTime(stageId);
        if (clearTime < minTime)
        {
            ReportSuspicious("clear_too_fast");
            return false;
        }
        
        // 전투력 대비 점수 체크
        int expectedMaxScore = CalculateMaxPossibleScore(stageId, _playerPower);
        if (score > expectedMaxScore * 1.2f)  // 20% 마진
        {
            ReportSuspicious("score_too_high");
            return false;
        }
        
        return true;
    }
}
```

---

## 12. 개발 일정

### 12.1 전체 타임라인 (20주)
```
Phase 1: Foundation (Week 1-4)
  - 프로젝트 셋업, 아키텍처
  - 핵심 시스템 프로토타입

Phase 2: Core Gameplay (Week 5-10)
  - 전투 시스템 완성
  - 슬라임 시스템 (30종)
  - 스테이지 10개

Phase 3: Meta Systems (Week 11-14)
  - 가챠 시스템
  - 합성 시스템
  - 업그레이드 시스템

Phase 4: Economy & UX (Week 15-17)
  - IAP 연동
  - 광고 연동
  - UI 폴리싱

Phase 5: QA & Launch (Week 18-20)
  - 밸런싱
  - 버그 수정
  - 소프트 런칭
  - 글로벌 런칭
```

### 12.2 주차별 마일스톤

#### Week 1-2: 프로젝트 셋업
```
□ Unity 프로젝트 생성 & 구조
□ PlayFab 연동
□ 기본 씬 구조
□ CI/CD (Unity Cloud Build)
□ 버전 관리 (Git Flow)
```

#### Week 3-4: 코어 프로토타입
```
□ 3x3 그리드 배치 시스템
□ 기본 전투 루프
□ 슬라임 엔티티 5종
□ 데미지 계산 로직
□ 간단한 적 AI
```

#### Week 5-8: 전투 시스템
```
□ 웨이브 시스템
□ 스킬 시스템
□ 속성 상성
□ 보스 전투
□ 전투 UI 완성
□ 전투 이펙트 & 사운드
□ 자동 전투 & 배속
```

#### Week 9-10: 콘텐츠
```
□ 슬라임 30종 완성
□ 스테이지 1-10 완성
□ 적 유닛 15종
□ 보스 1종
□ 밸런싱 1차
```

#### Week 11-12: 가챠 & 합성
```
□ 가챠 시스템 로직
□ 가챠 연출 (애니메이션)
□ 천장 시스템
□ 합성 시스템
□ 특수 레시피
□ 서버 검증
```

#### Week 13-14: 메타 시스템
```
□ 영구 업그레이드
□ 도감 시스템
□ 업적 시스템
□ 일일 퀘스트
□ 튜토리얼
```

#### Week 15-16: 수익화
```
□ IAP 상품 설정
□ StoreKit/Billing 연동
□ 광고 SDK 연동
□ 보상형 광고 포인트
□ 상점 UI
```

#### Week 17-18: 폴리싱
```
□ UI/UX 개선
□ 사운드 완성
□ 현지화 (한/영/일)
□ 성능 최적화
□ 메모리 최적화
```

#### Week 19-20: 런칭
```
□ 소프트 런칭 (필리핀)
□ 데이터 수집 & 분석
□ 밸런싱 조정
□ 버그 수정
□ 글로벌 런칭
```

---

## 13. 라이브 서비스 계획

### 13.1 런칭 후 로드맵
```
Month 1: 안정화
  - 버그 수정
  - 밸런스 패치
  - 신규 슬라임 5종

Month 2: 시즌 1
  - 시즌 패스 도입
  - 신규 챕터 (11-20 스테이지)
  - 이벤트 던전

Month 3: 소셜
  - 길드 시스템
  - 길드 레이드
  - 친구 시스템

Month 4-6: 확장
  - PvP 아레나
  - 월드 보스
  - 협동 컨텐츠
```

### 13.2 라이브 운영 주기
```
일일:
  - 무료 가챠 리셋
  - 일일 미션 리셋
  - 에너지 메일 발송 (오프라인 보상)

주간:
  - 주간 미션 리셋
  - 랭킹 정산
  - 주간 이벤트 교체

월간:
  - 시즌 정산
  - 신규 콘텐츠 업데이트
  - 밸런스 패치

분기:
  - 대규모 업데이트
  - 신규 시스템
  - 콜라보 이벤트
```

### 13.3 성과 지표 모니터링
```yaml
Daily Metrics:
  - DAU, New Users, Retention
  - ARPDAU, Revenue
  - Session Length, Sessions per Day
  - Tutorial Completion Rate

Weekly Metrics:
  - WAU, D7 Retention
  - Gacha Pull Rate
  - IAP Conversion
  - Ad Watch Rate

Monthly Metrics:
  - MAU, D30 Retention
  - LTV, ROAS
  - Paying User Rate
  - Churn Rate
```

---

*끝. 이 설계서를 기반으로 개발을 시작하세요!* 🟢
