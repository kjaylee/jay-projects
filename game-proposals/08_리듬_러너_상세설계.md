# 🎵 리듬 러너 - 상세설계서

## 1. 게임 시스템 상세

### 1.1 러닝 시스템

#### 레인 구조
```
[3레인 시스템]
┌─────┬─────┬─────┐
│ 좌  │ 중앙 │  우 │
│ (L) │ (M) │ (R) │
└─────┴─────┴─────┘

레인 너비: 화면 너비의 30% 씩
레인 전환 시간: 0.15초
레인 전환 이징: EaseOutQuad
```

#### 장애물 시스템
| 장애물 타입 | 회피 방법 | 충돌 판정 |
|------------|----------|----------|
| 낮은 장애물 | 점프 | Box Collider (h: 1.0) |
| 높은 장애물 | 슬라이드 | Box Collider (h: 2.5) |
| 좌우 장애물 | 레인 이동 | Box Collider (w: 레인 1개) |
| 이동 장애물 | 타이밍 | 좌우 이동 패턴 |

#### 속도 시스템
```csharp
// 속도 계산 공식
float currentSpeed = baseSpeed * (1 + (comboCount * 0.01f)) * feverMultiplier;

// 기본 값
baseSpeed = 8f;           // 유닛/초
maxSpeed = 15f;           // 속도 상한
feverMultiplier = 1.3f;   // 피버 시 속도 증가
```

### 1.2 리듬 시스템

#### 비트 노트 생성
```csharp
[System.Serializable]
public class BeatNote {
    public float timestamp;      // 노트 타이밍 (초)
    public int lane;             // 0=좌, 1=중앙, 2=우
    public NoteType type;        // Tap, Hold, Swipe
    public float duration;       // Hold 노트 지속시간
}

public enum NoteType {
    Tap,        // 단타
    Hold,       // 롱노트
    SwipeLeft,  // 좌스와이프
    SwipeRight  // 우스와이프
}
```

#### 판정 시스템
| 판정 | 타이밍 오차 (ms) | 점수 | 게이지 충전 |
|------|-----------------|------|------------|
| Perfect | ±30ms | 100점 | +15 |
| Great | ±60ms | 80점 | +10 |
| Good | ±100ms | 50점 | +5 |
| Miss | >100ms | 0점 | -20 |

```csharp
// 판정 로직
public JudgeResult Judge(float inputTime, float noteTime) {
    float diff = Mathf.Abs(inputTime - noteTime) * 1000f; // ms
    
    if (diff <= 30f) return JudgeResult.Perfect;
    if (diff <= 60f) return JudgeResult.Great;
    if (diff <= 100f) return JudgeResult.Good;
    return JudgeResult.Miss;
}
```

#### 콤보 시스템
```
콤보 보너스 = floor(comboCount / 10) * 10%
최대 콤보 보너스: 100% (100콤보 이상)

콤보 리셋 조건:
- Miss 판정
- 장애물 충돌
```

### 1.3 피버 시스템

#### 게이지 충전
```
최대 게이지: 100
Perfect: +15
Great: +10
Good: +5
Miss: -20 (최소 0)

게이지 100 도달 시 피버 자동 발동
```

#### 피버 효과
| 효과 | 값 |
|------|-----|
| 지속시간 | 10초 (기본) |
| 점수 배율 | x2.0 |
| 무적 상태 | 장애물 통과 |
| 속도 증가 | x1.3 |
| 비주얼 | 네온 글로우 + 파티클 |

#### 캐릭터별 피버 보너스
| 캐릭터 | 피버 특수 효과 |
|--------|---------------|
| 록스타 | 피버 후 Miss 1회 무효 |
| 댄서 | 피버 점수 x2.5 |
| 로봇 DJ | 피버 +3초 |
| 네온 고스트 | 피버 게이지 소모 70% |

### 1.4 스테이지 구조

#### 곡 데이터 구조
```csharp
[CreateAssetMenu(fileName = "Song", menuName = "Rhythm/Song Data")]
public class SongData : ScriptableObject {
    public string songTitle;
    public string artist;
    public AudioClip audioClip;
    public float bpm;
    public float offset;          // 첫 비트 오프셋
    public Difficulty[] difficulties;
    public Sprite albumArt;
    public WorldTheme theme;
}

[System.Serializable]
public class Difficulty {
    public DifficultyLevel level;
    public List<BeatNote> notes;
    public int starThresholds;    // 별 획득 기준점수
}
```

#### 월드 테마별 환경
| 월드 | 환경 특징 | 장애물 스킨 | 특수 기믹 |
|------|----------|------------|----------|
| 시티 나이트 | 네온 빌딩, 자동차 | 표지판, 택시 | 신호등 변화 |
| 도쿄 스트릿 | 일본 거리, 벚꽃 | 자판기, 간판 | 열차 통과 |
| 서울 바이브 | 한국 거리, LED | K-POP 포스터 | 팬 응원봉 |
| 유럽 클래식 | 오페라 하우스 | 악기, 악보대 | 지휘자 박자 |
| 네온 드림 | 사이버펑크 | 홀로그램 | 글리치 효과 |

---

## 2. UI/UX 설계

### 2.1 인게임 UI 레이아웃
```
┌────────────────────────────────────┐
│ ⏸ [Score: 125,600]    🔥x15       │ ← 상단 HUD
├────────────────────────────────────┤
│                                    │
│     ╔═══════════════════════╗     │
│     ║    [비트 노트 영역]    ║     │ ← 노트 낙하 구간
│     ║    ● ○ ●              ║     │
│     ╚═══════════════════════╝     │
│                                    │
│  ━━━━━━━━[판정선]━━━━━━━━         │ ← 판정 라인
│                                    │
│         🏃 [캐릭터]               │
│    ─────────────────────          │ ← 달리는 트랙
│    [좌] [중앙] [우]               │
│                                    │
├────────────────────────────────────┤
│ [피버 게이지 ████████░░░░ 75%]    │ ← 하단 게이지
└────────────────────────────────────┘
```

### 2.2 판정 피드백 UI
```
Perfect: 
- 텍스트: 골드 색상, 스케일 1.5 → 1.0
- 파티클: 별 이펙트
- 진동: 20ms (약함)

Great:
- 텍스트: 흰색, 스케일 1.3 → 1.0
- 파티클: 작은 반짝임
- 진동: 15ms

Miss:
- 텍스트: 빨강, 흔들림 효과
- 화면: 빨간 테두리 플래시
- 진동: 50ms (강함)
```

### 2.3 메뉴 플로우
```
[메인 메뉴]
    │
    ├─→ [플레이]
    │       │
    │       ├─→ [월드 선택] → [곡 선택] → [난이도 선택] → [게임]
    │       └─→ [빠른 플레이] → [게임]
    │
    ├─→ [캐릭터]
    │       └─→ [캐릭터 목록] → [상세/구매]
    │
    ├─→ [상점]
    │       └─→ [곡 팩 / 캐릭터 / 스킨]
    │
    └─→ [설정]
            └─→ [음량 / 판정 보정 / 진동 / 언어]
```

### 2.4 곡 선택 화면
```
┌────────────────────────────────────┐
│ ← 월드: 시티 나이트           ⚙️  │
├────────────────────────────────────┤
│                                    │
│  ┌──────┐ ┌──────┐ ┌──────┐      │
│  │ 🔒   │ │ ★★☆ │ │ ★★★ │      │ ← 앨범 아트
│  │ EDM  │ │Remix │ │Night │      │   잠금/별 표시
│  └──────┘ └──────┘ └──────┘      │
│                                    │
│  [선택된 곡 정보]                  │
│  🎵 Night Drive                   │
│  🎤 DJ Electric                   │
│  ⏱️ 2:35  BPM: 128                │
│                                    │
│  난이도:                          │
│  [Easy] [Normal] [Hard] [Expert]  │
│        ⭐⭐      ⭐⭐⭐     ⭐⭐⭐⭐⭐  │
│                                    │
│  최고 기록: 125,600 (S랭크)       │
│                                    │
├────────────────────────────────────┤
│      [🎧 미리듣기]  [▶ 플레이]    │
└────────────────────────────────────┘
```

---

## 3. 밸런싱 수치

### 3.1 점수 시스템
```
기본 점수 = 판정 점수 × (1 + 콤보 보너스) × 피버 배율

곡별 이론 최대 점수 계산:
- 노트 수 × 100 (모든 Perfect)
- × 2.0 (100콤보 보너스)
- × 2.0 (피버 발동 시)

예시: 200노트 곡
이론치 = 200 × 100 × 2.0 × 2.0 = 80,000점
```

### 3.2 랭크 기준
| 랭크 | 점수 비율 | 설명 |
|------|----------|------|
| SSS | 100% | 올 퍼펙트 |
| SS | 95%+ | 거의 완벽 |
| S | 90%+ | 우수 |
| A | 80%+ | 양호 |
| B | 70%+ | 보통 |
| C | 60%+ | 클리어 |
| F | <60% | 실패 |

### 3.3 별 획득 조건
| 별 | 조건 |
|-----|------|
| ⭐ | 클리어 (60% 이상) |
| ⭐⭐ | 80% 이상 |
| ⭐⭐⭐ | 95% 이상 |

### 3.4 에너지 시스템
```
최대 에너지: 5
충전 속도: 10분 / 1에너지
소모량: 1 / 1플레이
광고 충전: +1 에너지 (3회/일)
VIP 패스: 무제한
```

### 3.5 재화 획득량
| 출처 | 골드 획득 |
|------|----------|
| 곡 클리어 (Easy) | 50 |
| 곡 클리어 (Normal) | 100 |
| 곡 클리어 (Hard) | 150 |
| 곡 클리어 (Expert) | 200 |
| 첫 클리어 보너스 | +100 |
| S랭크 보너스 | +50 |
| 일일 퀘스트 | 300/일 |

### 3.6 캐릭터 해금 비용
| 등급 | 골드 가격 | 젬 가격 | USD 환산 |
|------|----------|---------|---------|
| 기본 | 무료 | - | - |
| 레어 | 5,000 | 500 | $1.99 |
| 에픽 | 15,000 | 1,500 | $4.99 |
| 레전더리 | - | 3,000 | $9.99 |

---

## 4. 기술 구현 사항

### 4.1 오디오 동기화
```csharp
public class AudioSyncManager : MonoBehaviour {
    private AudioSource audioSource;
    private double dspStartTime;
    
    // 정밀 타이밍을 위한 DSP 시간 사용
    public double GetPreciseTime() {
        return AudioSettings.dspTime - dspStartTime;
    }
    
    public void StartSong(AudioClip clip, float offset) {
        dspStartTime = AudioSettings.dspTime + offset;
        audioSource.PlayScheduled(dspStartTime);
    }
    
    // 판정 보정값 적용
    public float GetAdjustedTime(float calibrationOffset) {
        return (float)GetPreciseTime() + calibrationOffset;
    }
}
```

### 4.2 노트 풀링 시스템
```csharp
public class NotePool : MonoBehaviour {
    [SerializeField] private int poolSize = 50;
    private Queue<Note> pool = new Queue<Note>();
    
    void Awake() {
        // 사전 생성
        for (int i = 0; i < poolSize; i++) {
            Note note = Instantiate(notePrefab);
            note.gameObject.SetActive(false);
            pool.Enqueue(note);
        }
    }
    
    public Note GetNote() {
        if (pool.Count > 0) {
            Note note = pool.Dequeue();
            note.gameObject.SetActive(true);
            return note;
        }
        return Instantiate(notePrefab); // 풀 소진 시 새로 생성
    }
    
    public void ReturnNote(Note note) {
        note.gameObject.SetActive(false);
        pool.Enqueue(note);
    }
}
```

### 4.3 비트맵 에디터 (에디터 확장)
```csharp
#if UNITY_EDITOR
public class BeatmapEditor : EditorWindow {
    private SongData currentSong;
    private float currentTime;
    private float zoom = 100f; // 픽셀/초
    
    void OnGUI() {
        // 타임라인 그리기
        DrawTimeline();
        
        // 오디오 파형 표시
        DrawWaveform();
        
        // 노트 배치/편집
        DrawNotes();
        
        // BPM 그리드
        DrawBPMGrid();
    }
    
    void DrawBPMGrid() {
        float beatInterval = 60f / currentSong.bpm;
        // 비트마다 세로선 그리기
    }
}
#endif
```

### 4.4 유저 곡 BPM 분석 (선택적)
```csharp
public class BPMAnalyzer {
    public float AnalyzeBPM(AudioClip clip) {
        float[] samples = new float[clip.samples];
        clip.GetData(samples, 0);
        
        // 에너지 피크 감지
        List<float> peaks = DetectPeaks(samples, clip.frequency);
        
        // 피크 간격으로 BPM 계산
        float avgInterval = CalculateAverageInterval(peaks);
        float bpm = 60f / avgInterval;
        
        // 일반적인 BPM 범위로 정규화 (60-200)
        while (bpm < 60) bpm *= 2;
        while (bpm > 200) bpm /= 2;
        
        return bpm;
    }
}
```

### 4.5 판정 보정 시스템
```csharp
public class CalibrationManager : MonoBehaviour {
    public float inputOffset = 0f;  // 사용자 입력 보정
    public float audioOffset = 0f;  // 오디오 출력 보정
    
    // 자동 보정 테스트
    public IEnumerator RunCalibrationTest() {
        List<float> offsets = new List<float>();
        
        for (int i = 0; i < 8; i++) {
            // 비트 재생
            PlayCalibrationBeat();
            
            // 사용자 탭 대기
            yield return WaitForTap();
            
            // 오차 기록
            offsets.Add(actualTime - expectedTime);
        }
        
        // 평균 오프셋 계산 (이상치 제거)
        inputOffset = CalculateTrimmedMean(offsets);
    }
}
```

### 4.6 데이터 저장 구조
```csharp
[System.Serializable]
public class PlayerData {
    public int totalGold;
    public int gems;
    public List<string> unlockedCharacters;
    public string currentCharacter;
    public Dictionary<string, SongRecord> songRecords;
    public float calibrationOffset;
    public int energy;
    public DateTime lastEnergyTime;
}

[System.Serializable]
public class SongRecord {
    public int highScore;
    public string rank;
    public int stars;
    public int playCount;
    public bool[] difficultyCleared; // Easy, Normal, Hard, Expert
}
```

### 4.7 성능 최적화

#### 타겟 성능
| 항목 | 목표 |
|------|------|
| FPS | 60 FPS 고정 |
| 입력 지연 | <16ms |
| 오디오 지연 | <20ms |
| 메모리 | <500MB |
| APK 크기 | <150MB (기본곡 포함) |

#### 최적화 기법
```
1. 노트 오브젝트 풀링
2. 배경 스크롤 - 타일링 텍스처 사용
3. 파티클 - GPU 인스턴싱
4. 오디오 - 스트리밍 로드 (곡 파일)
5. UI - 캔버스 분리 (정적/동적)
6. 프로파일링 - 판정 로직 최적화
```

### 4.8 서버 연동 (선택적)
```
[Firebase 구성]
- Authentication: 익명 로그인 + 소셜 연동
- Firestore: 리더보드, 유저 데이터
- Remote Config: 이벤트, 밸런스 조정
- Analytics: 플레이 데이터 수집

[API 엔드포인트]
POST /api/score/submit     - 점수 제출
GET  /api/leaderboard/{songId} - 리더보드 조회
GET  /api/songs/list       - 곡 목록 (DLC 포함)
POST /api/purchase/verify  - IAP 검증
```

---

## 5. QA 체크리스트

### 5.1 코어 게임플레이
- [ ] 모든 판정 타이밍 정확성
- [ ] 콤보 카운트 정확성
- [ ] 피버 게이지 충전/발동
- [ ] 장애물 충돌 판정
- [ ] 레인 이동 반응성

### 5.2 오디오
- [ ] 음악-노트 동기화 (모든 곡)
- [ ] 배경음악 음량
- [ ] 효과음 타이밍
- [ ] 판정 보정 기능

### 5.3 UI/UX
- [ ] 터치 반응 (16ms 이내)
- [ ] 해상도별 UI 스케일링
- [ ] 노치/펀치홀 대응
- [ ] 언어별 텍스트 오버플로우

### 5.4 성능
- [ ] 60FPS 유지 (저사양 기기)
- [ ] 메모리 누수 없음
- [ ] 배터리 소모량
- [ ] 발열

---

*작성일: 2026년 1월 27일*
*버전: 1.0*
