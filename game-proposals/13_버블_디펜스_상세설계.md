# 버블 디펜스 (Bubble Defense) 상세 설계서

## 📐 게임 시스템 설계

### 1. 버블 시스템

#### 버블 색상 (5종)
| 색상 | 코드 | 에너지 | 특수 효과 |
|------|------|--------|-----------|
| 🔴 빨강 | #FF4444 | 10 | 화염 타워 보너스 +20% |
| 🔵 파랑 | #4488FF | 10 | 얼음 타워 보너스 +20% |
| 🟢 초록 | #44DD44 | 10 | 독 타워 보너스 +20% |
| 🟡 노랑 | #FFDD44 | 10 | 번개 타워 보너스 +20% |
| 🟣 보라 | #AA44FF | 10 | 마법 타워 보너스 +20% |

#### 특수 버블
| 종류 | 획득 방법 | 효과 |
|------|-----------|------|
| 🌈 레인보우 | 5개 이상 매치 | 모든 색 대체 |
| 💣 폭탄 | 광고/구매 | 범위 3칸 파괴 |
| ⚡ 번개 | 랜덤 드롭 | 세로줄 전체 파괴 |
| 🔥 화염 | 6개 이상 매치 | 인접 버블 추가 파괴 |

#### 버블 그리드
- **크기:** 9열 × 12행
- **배치:** 벌집(hexagonal) 구조
- **상단 생성:** 웨이브마다 1-2줄 추가
- **하단 경계:** 경계선 넘으면 게임 오버

### 2. 타워 시스템

#### 타워 종류 (5종)
| 타워 | 비용 | 공격력 | 공격속도 | 범위 | 특수 |
|------|------|--------|----------|------|------|
| 🏹 아처 | 50 | 10 | 1.0s | 중 | 기본 공격 |
| 🔥 화염 | 80 | 15 | 1.5s | 중 | 화상 DoT |
| ❄️ 얼음 | 80 | 8 | 1.2s | 중 | 30% 슬로우 |
| ⚡ 번개 | 100 | 20 | 2.0s | 넓음 | 연쇄 3타겟 |
| 💀 독 | 100 | 5 | 0.8s | 좁음 | 독 스택 |

#### 타워 업그레이드
- **레벨 1→2:** 비용 50%, 공격력 +30%
- **레벨 2→3:** 비용 100%, 공격력 +50%, 특수효과 강화
- **최대 레벨:** 3

#### 타워 배치
- **배치 슬롯:** 화면 하단 4칸 (확장 가능)
- **배치 비용:** 에너지 소모
- **판매:** 비용의 50% 회수

### 3. 적 시스템

#### 적 종류
| 적 | 체력 | 이동속도 | 특성 | 골드 |
|----|------|----------|------|------|
| 🟢 슬라임 | 30 | 보통 | 없음 | 5 |
| 🔵 고블린 | 50 | 빠름 | 없음 | 8 |
| 🟠 오크 | 100 | 느림 | 높은 체력 | 15 |
| 🟣 마법사 | 40 | 보통 | 타워 약화 | 12 |
| 🔴 보스 | 500 | 느림 | 면역 단계 | 50 |

#### 적 경로
- 화면 상단에서 하단으로 이동
- 경로는 S자 또는 지그재그
- 기지(하단)에 도달하면 체력 감소

### 4. 웨이브 시스템

#### 웨이브 구성
```
웨이브 1: 슬라임 5마리
웨이브 2: 슬라임 8마리
웨이브 3: 슬라임 5 + 고블린 3
웨이브 4: 고블린 8마리
웨이브 5: 슬라임 5 + 오크 2 (보스 웨이브)
```

#### 난이도 스케일링
- **웨이브당:** 적 체력 +10%, 적 수 +1
- **스테이지당:** 새로운 적 타입 등장

---

## 🎨 UI/UX 설계

### 1. 화면 레이아웃

```
┌─────────────────────────────────────┐
│  ❤️ 100   ⚡ 250   🪙 500   ⏸️     │  ← 상태바
├─────────────────────────────────────┤
│                                     │
│       ● ● ● ● ● ● ● ● ●           │
│        ● ● ● ● ● ● ● ●            │  ← 버블 그리드
│       ● ● ● ● ● ● ● ● ●           │     (상단 2/3)
│        ● ● ● ● ● ● ● ●            │
│       ● ● ● ● ● ● ● ● ●           │
│                                     │
│  ─────────경로───────────           │
│     👾    👾         👾             │  ← 적 이동 경로
│  ───────────────────────           │
├─────────────────────────────────────┤
│  [🏹] [🔥] [❄️] [⚡] [💀]          │  ← 타워 슬롯
├─────────────────────────────────────┤
│                                     │
│            ●                        │  ← 발사대
│           /                         │
│          ●                          │  ← 대기 버블
│                                     │
└─────────────────────────────────────┘
```

### 2. HUD 요소

#### 상태바 (상단)
- **❤️ 체력:** 기지 체력 (100/100)
- **⚡ 에너지:** 타워 건설용 (0-999)
- **🪙 골드:** 영구 재화
- **⏸️ 일시정지:** 메뉴 접근

#### 웨이브 정보
- **웨이브 번호:** "Wave 3/10"
- **남은 적 수:** "Enemies: 5"
- **다음 웨이브 타이머:** (선택적 자동 진행 시)

### 3. 조작 인터페이스

#### 버블 발사
- **조준:** 손가락 드래그 (가이드라인 표시)
- **발사:** 손가락 떼기
- **버블 교체:** 대기 버블 터치

#### 타워 배치
- **선택:** 하단 타워 아이콘 터치
- **배치:** 슬롯으로 드래그 앤 드롭
- **업그레이드:** 배치된 타워 터치 → 업그레이드 버튼
- **판매:** 배치된 타워 길게 터치

### 4. 피드백 시스템

#### 시각 피드백
- **버블 매치:** 팝 이펙트 + 파티클
- **에너지 획득:** 숫자 플로팅 (+10)
- **타워 공격:** 투사체 애니메이션
- **적 피해:** 체력바 감소 + 플래시
- **적 처치:** 폭발 이펙트 + 골드 획득

#### 청각 피드백
- **버블 발사:** "핑"
- **버블 매치:** "퐁퐁퐁" (연속음)
- **타워 공격:** 타워별 고유 사운드
- **적 처치:** "펑"
- **웨이브 클리어:** 승리 팡파레

---

## ⚖️ 밸런싱 수치

### 1. 경제 밸런스

#### 에너지 획득
| 매치 수 | 에너지 | 보너스 |
|---------|--------|--------|
| 3개 | 30 | - |
| 4개 | 50 | +20 |
| 5개 | 80 | +30 + 레인보우 |
| 6개+ | 120 | +40 + 화염버블 |

#### 골드 획득
- 적 처치: 5-50 (타입별)
- 웨이브 클리어 보너스: 웨이브 × 10
- 스테이지 클리어: 100 + (별 × 50)

### 2. 난이도 곡선

#### 스테이지별 설정
| 스테이지 | 웨이브 수 | 적 체력 배율 | 신규 요소 |
|----------|-----------|--------------|-----------|
| 1 | 5 | 1.0× | 튜토리얼 |
| 2 | 6 | 1.2× | 고블린 등장 |
| 3 | 7 | 1.4× | 화염타워 해금 |
| 4 | 8 | 1.6× | 오크 등장 |
| 5 | 10 | 2.0× | 첫 보스 |

#### 버블 하강 속도
- 웨이브 1-3: 없음
- 웨이브 4-6: 느림 (10초마다 1줄)
- 웨이브 7-9: 보통 (7초마다 1줄)
- 웨이브 10: 빠름 (5초마다 1줄)

### 3. 타워 효율

#### 골드 대비 DPS
| 타워 | 비용 | DPS | 효율 |
|------|------|-----|------|
| 아처 | 50 | 10 | 0.20 |
| 화염 | 80 | 12 | 0.15 |
| 얼음 | 80 | 6.7 | 0.08 (유틸) |
| 번개 | 100 | 30 | 0.30 (멀티) |
| 독 | 100 | 25 | 0.25 (스택) |

---

## 🔧 기술 구현 사항

### 1. 렌더링

#### Canvas 2D
```javascript
// 기본 설정
const canvas = document.getElementById('game');
const ctx = canvas.getContext('2d');
const BUBBLE_RADIUS = 20;
const GRID_COLS = 9;
const GRID_ROWS = 12;
```

#### 프레임 관리
- **목표 FPS:** 60
- **requestAnimationFrame 사용**
- **델타 타임 기반 업데이트**

### 2. 버블 물리

#### 발사 계산
```javascript
function shootBubble(angle, power) {
    const vx = Math.cos(angle) * power;
    const vy = Math.sin(angle) * power;
    return { vx, vy };
}
```

#### 충돌 감지
- 버블 간: 원-원 충돌
- 벽: 반사 (X축 방향 반전)
- 천장: 고정

#### 매칭 알고리즘
```javascript
function findMatches(bubble) {
    const matches = [];
    const visited = new Set();
    const queue = [bubble];
    
    while (queue.length > 0) {
        const current = queue.shift();
        if (visited.has(current.id)) continue;
        visited.add(current.id);
        
        if (current.color === bubble.color) {
            matches.push(current);
            // 인접 버블 큐에 추가
            getNeighbors(current).forEach(n => queue.push(n));
        }
    }
    return matches;
}
```

### 3. 타워 AI

#### 타겟팅 우선순위
1. 가장 앞선 적 (First)
2. 체력 낮은 적 (Weak)
3. 체력 높은 적 (Strong)
4. 가장 가까운 적 (Close)

#### 공격 로직
```javascript
class Tower {
    update(deltaTime, enemies) {
        this.cooldown -= deltaTime;
        if (this.cooldown <= 0) {
            const target = this.findTarget(enemies);
            if (target && this.inRange(target)) {
                this.attack(target);
                this.cooldown = this.attackSpeed;
            }
        }
    }
}
```

### 4. 적 경로 이동

#### 웨이포인트 시스템
```javascript
const PATH = [
    { x: 50, y: 300 },
    { x: 250, y: 350 },
    { x: 50, y: 400 },
    { x: 250, y: 450 },
    { x: 150, y: 500 }  // 목적지
];

class Enemy {
    move(deltaTime) {
        const target = PATH[this.waypointIndex];
        const dx = target.x - this.x;
        const dy = target.y - this.y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        
        if (dist < 5) {
            this.waypointIndex++;
        } else {
            this.x += (dx/dist) * this.speed * deltaTime;
            this.y += (dy/dist) * this.speed * deltaTime;
        }
    }
}
```

### 5. 오디오 시스템

#### Web Audio API
```javascript
const audioContext = new AudioContext();

function playSound(frequency, duration, type = 'sine') {
    const oscillator = audioContext.createOscillator();
    const gainNode = audioContext.createGain();
    
    oscillator.type = type;
    oscillator.frequency.value = frequency;
    oscillator.connect(gainNode);
    gainNode.connect(audioContext.destination);
    
    gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
    gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + duration);
    
    oscillator.start();
    oscillator.stop(audioContext.currentTime + duration);
}
```

### 6. 저장 시스템

#### LocalStorage
```javascript
const SaveManager = {
    save(data) {
        localStorage.setItem('bubbleDefense', JSON.stringify({
            gold: data.gold,
            highScore: data.highScore,
            unlockedTowers: data.unlockedTowers,
            currentStage: data.currentStage
        }));
    },
    
    load() {
        const saved = localStorage.getItem('bubbleDefense');
        return saved ? JSON.parse(saved) : null;
    }
};
```

---

## 📦 에셋 활용 계획

### Kenney.nl 에셋 (CC0)

#### Puzzle Pack 2
- 색상 버블 스프라이트
- UI 버튼/패널
- 파티클 이펙트

#### Tower Defense (대안)
- 타워 스프라이트 (또는 자체 SVG 생성)
- 적 캐릭터
- 경로 타일

### 자체 제작 (SVG/Canvas)
- 버블: 단순 원형 + 그라디언트
- 타워: 기하학적 도형 조합
- 적: 슬라임 모양 (원형 변형)
- 이펙트: Canvas 파티클

### 사운드 (Web Audio API)
- 발사음: 높은 주파수 핑
- 매치음: 상승 아르페지오
- 폭발음: 화이트 노이즈 + 디케이
- 배경음: 루프 멜로디 (선택적)

---

## 🎯 MVP 체크리스트

### 필수 기능
- [x] 버블 그리드 생성
- [x] 버블 발사 (드래그 조준)
- [x] 매치-3 감지 및 파괴
- [x] 에너지 시스템
- [x] 3종 타워 (아처, 화염, 얼음)
- [x] 적 스폰 및 경로 이동
- [x] 타워 공격
- [x] 웨이브 시스템 (5 웨이브)
- [x] 게임 오버/클리어 조건
- [x] 기본 UI

### 선택 기능
- [ ] 타워 업그레이드
- [ ] 특수 버블
- [ ] 보스
- [ ] 사운드 효과
- [ ] 세이브/로드
- [ ] 다중 스테이지
