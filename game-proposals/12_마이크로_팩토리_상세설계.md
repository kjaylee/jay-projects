# 마이크로 팩토리 (Micro Factory) 상세 설계서

## 1. 게임 시스템 설계

### 1.1 그리드 시스템
- **그리드 크기**: 12x8 (모바일 최적화)
- **셀 크기**: 화면 너비 / 12
- **배치 규칙**: 한 셀에 하나의 오브젝트

### 1.2 오브젝트 타입

#### 자원 노드 (Resource Nodes)
| 노드 | 색상 | 생성 자원 |
|------|------|-----------|
| 철광석 | 회색 | Iron Ore |
| 구리광석 | 주황색 | Copper Ore |
| 석탄 | 검정색 | Coal |

#### 기계 (Machines)
| 기계 | 기능 | 처리시간 | 해금 조건 |
|------|------|----------|-----------|
| 채굴기 | 자원 노드에서 광석 채굴 | 2초 | 기본 |
| 제련소 | 광석 → 주괴 | 3초 | 레벨 2 |
| 조립기 | 주괴 → 제품 | 4초 | 레벨 5 |
| 판매소 | 제품 → 골드 | 즉시 | 기본 |

#### 컨베이어 (Conveyor)
- **방향**: 4방향 (상하좌우)
- **속도**: 1셀/초 (업그레이드 가능)
- **용량**: 한 번에 1개 아이템

### 1.3 생산 레시피

```javascript
const RECIPES = {
  // 기본 레시피
  ironIngot: { input: ['ironOre'], output: 'ironIngot', time: 3 },
  copperIngot: { input: ['copperOre'], output: 'copperIngot', time: 3 },
  
  // 중급 레시피
  gear: { input: ['ironIngot', 'ironIngot'], output: 'gear', time: 4 },
  wire: { input: ['copperIngot'], output: 'wire', time: 2 },
  
  // 고급 레시피
  circuit: { input: ['wire', 'ironIngot'], output: 'circuit', time: 5 },
  motor: { input: ['gear', 'wire'], output: 'motor', time: 6 },
  
  // 최종 제품
  robot: { input: ['circuit', 'motor', 'gear'], output: 'robot', time: 10 }
};
```

### 1.4 판매 가격표

| 아이템 | 가격 |
|--------|------|
| Iron Ore | 1 |
| Copper Ore | 1 |
| Iron Ingot | 5 |
| Copper Ingot | 5 |
| Gear | 15 |
| Wire | 8 |
| Circuit | 30 |
| Motor | 40 |
| Robot | 100 |

---

## 2. UI/UX 설계

### 2.1 메인 화면 레이아웃

```
┌─────────────────────────────────────┐
│  💰 1,234    ⭐ Lv.5    ⚡ x1.0     │  ← 상단 HUD
├─────────────────────────────────────┤
│                                     │
│                                     │
│         [12x8 Factory Grid]         │  ← 메인 그리드
│                                     │
│                                     │
├─────────────────────────────────────┤
│ [🔨][⛏️][📦][🔄][⬆️]               │  ← 하단 빌드바
└─────────────────────────────────────┘
```

### 2.2 빌드 모드 UI

```
┌───────────────────┐
│   Build Menu      │
├───────────────────┤
│ ⛏️ Miner     50💰 │
│ 🔥 Smelter  100💰 │
│ ⚙️ Assembler 200💰 │
│ 📦 Seller    Free │
│ ➡️ Conveyor  10💰 │
└───────────────────┘
```

### 2.3 터치 인터랙션
- **탭**: 오브젝트 선택/정보 표시
- **드래그**: 컨베이어 연속 배치
- **롱프레스**: 삭제 모드
- **핀치**: 줌 (선택적)

### 2.4 시각적 피드백
- 생산 중: 기계 위 진행률 바
- 아이템 이동: 컨베이어 위 스프라이트 이동
- 병목: 붉은 경고 아이콘
- 완료: 파티클 이펙트

---

## 3. 밸런싱 수치

### 3.1 경제 밸런싱

```javascript
const BALANCE = {
  // 초기 자본
  startGold: 200,
  
  // 기계 가격 (레벨별 스케일링)
  machineCost: {
    miner: (lv) => 50 * Math.pow(1.5, lv),
    smelter: (lv) => 100 * Math.pow(1.5, lv),
    assembler: (lv) => 200 * Math.pow(1.5, lv),
    conveyor: 10 // 고정
  },
  
  // 업그레이드 효과
  speedUpgrade: 1.2,  // 20% 속도 증가
  
  // 레벨업 요구사항
  levelExp: (lv) => 100 * Math.pow(2, lv)
};
```

### 3.2 시간 밸런싱

| 마일스톤 | 예상 도달 시간 |
|----------|---------------|
| 첫 주괴 생산 | 1분 |
| 첫 제품 완성 | 3분 |
| 첫 로봇 생산 | 10분 |
| 레벨 10 도달 | 30분 |
| 완전 자동화 | 1시간 |

---

## 4. 기술 구현 사항

### 4.1 렌더링

```javascript
// 캔버스 기반 렌더링
const canvas = document.getElementById('game');
const ctx = canvas.getContext('2d');

// 60fps 게임 루프
function gameLoop(timestamp) {
  update(timestamp);
  render();
  requestAnimationFrame(gameLoop);
}
```

### 4.2 상태 관리

```javascript
const gameState = {
  gold: 200,
  level: 1,
  exp: 0,
  grid: new Array(12 * 8).fill(null),
  items: [],  // 컨베이어 위 아이템들
  machines: [] // 활성 기계들
};
```

### 4.3 저장/로드

```javascript
// LocalStorage 기반 자동 저장
function saveGame() {
  localStorage.setItem('microFactory', JSON.stringify(gameState));
}

// 30초마다 자동 저장
setInterval(saveGame, 30000);
```

### 4.4 성능 최적화
- **Object Pooling**: 아이템 스프라이트 재사용
- **Dirty Rect**: 변경된 영역만 다시 그리기
- **RequestAnimationFrame**: 브라우저 최적화 활용

---

## 5. 사운드 설계

### 5.1 효과음 (Web Audio API)
| 이벤트 | 사운드 |
|--------|--------|
| 배치 성공 | 짧은 클릭음 |
| 채굴 완료 | 메탈릭 칭 |
| 제련 완료 | 용광로 쉬익 |
| 판매 | 코인 소리 |
| 레벨업 | 팡파르 |

### 5.2 배경음
- 공장 앰비언스 (기계 돌아가는 소리)
- 선택적 BGM

---

## 6. 에셋 명세

### 6.1 스프라이트 (Kenney.nl - CC0)
- **타일셋**: Factory/Industrial 팩
- **UI**: UI Pack (버튼, 패널)
- **아이콘**: Game Icons 팩

### 6.2 색상 팔레트
```css
:root {
  --bg-color: #1a1a2e;      /* 어두운 배경 */
  --grid-color: #16213e;    /* 그리드 라인 */
  --accent: #e94560;        /* 강조색 */
  --gold: #ffd700;          /* 골드 UI */
  --iron: #a8a8a8;          /* 철 */
  --copper: #cd7f32;        /* 구리 */
}
```

### 6.3 폰트
- **UI**: 'Orbitron' (Google Fonts) - 테크/공장 느낌
- **숫자**: 'Press Start 2P' - 레트로 픽셀 숫자
