# Orbit Striker 상세 설계서

## 1. 게임 시스템 설계

### 1.1 물리 엔진

#### 중력 시뮬레이션
```javascript
// 중력 상수
const GRAVITY_CONSTANT = 500;
const PLANET_MASS = 1000;

// 탄환 업데이트
function updateBullet(bullet, planet, dt) {
  // 거리 계산
  const dx = planet.x - bullet.x;
  const dy = planet.y - bullet.y;
  const distance = Math.sqrt(dx * dx + dy * dy);
  
  // 중력 가속도 (역제곱 법칙)
  const force = GRAVITY_CONSTANT * PLANET_MASS / (distance * distance);
  
  // 방향 정규화
  const ax = (dx / distance) * force;
  const ay = (dy / distance) * force;
  
  // 속도 적용
  bullet.vx += ax * dt;
  bullet.vy += ay * dt;
  
  // 위치 업데이트
  bullet.x += bullet.vx * dt;
  bullet.y += bullet.vy * dt;
}
```

#### 궤도 이동 (적)
```javascript
function updateEnemy(enemy, dt) {
  // 각속도 기반 회전
  enemy.angle += enemy.angularSpeed * dt;
  
  // 극좌표 → 직교좌표
  enemy.x = centerX + enemy.orbitRadius * Math.cos(enemy.angle);
  enemy.y = centerY + enemy.orbitRadius * Math.sin(enemy.angle);
}
```

### 1.2 충돌 시스템

```javascript
// 원형 충돌 검사
function checkCollision(a, b) {
  const dx = a.x - b.x;
  const dy = a.y - b.y;
  const distance = Math.sqrt(dx * dx + dy * dy);
  return distance < (a.radius + b.radius);
}

// 탄환-적 충돌
function processBulletEnemyCollision(bullet, enemy) {
  if (checkCollision(bullet, enemy)) {
    enemy.hp -= bullet.damage;
    if (!bullet.piercing) bullet.active = false;
    
    // 히트 이펙트
    spawnParticles(enemy.x, enemy.y, 10);
    playSound('hit');
    
    if (enemy.hp <= 0) {
      destroyEnemy(enemy);
    }
  }
}
```

---

## 2. UI/UX 설계

### 2.1 화면 레이아웃

```
┌─────────────────────────────────┐
│  WAVE: 5    SCORE: 12,450       │  ← 상단 HUD
│  ⚡ ⚡ ⚡ ⚡ ⚡                      │  ← 에너지바
├─────────────────────────────────┤
│                                 │
│      ●    ○       ○            │  ← 외곽 궤도 적
│          ╭───╮                  │
│    ○    │ 🌍 │    ○            │  ← 행성 + 캐논
│          ╰───╯                  │
│      ○         ●                │  ← 내부 궤도 적
│                                 │
│          ↗ (조준선)              │
├─────────────────────────────────┤
│  [PAUSE]              [SOUND]   │  ← 하단 버튼
└─────────────────────────────────┘
```

### 2.2 조작 방식

| 입력 | 동작 |
|-----|------|
| 터치 시작 | 조준 시작 |
| 드래그 | 방향/힘 조절 |
| 터치 종료 | 발사 |
| 더블탭 | 특수 능력 발동 |

### 2.3 색상 팔레트

```css
:root {
  --bg-space: #0a0a1a;
  --planet-core: #4a90d9;
  --planet-glow: #6bb3ff;
  --bullet: #ffdd44;
  --enemy-red: #ff4466;
  --enemy-green: #44ff88;
  --energy: #00ffcc;
  --ui-white: #ffffff;
}
```

---

## 3. 밸런싱 수치

### 3.1 플레이어 스탯

| 항목 | 기본값 | 최대 강화 |
|-----|--------|----------|
| 발사 쿨다운 | 0.5초 | 0.2초 |
| 탄환 속도 | 400 | 600 |
| 탄환 데미지 | 1 | 3 |
| 탄환 크기 | 8px | 16px |

### 3.2 웨이브 밸런싱

| 웨이브 | 적 수 | 적 HP 배율 | 적 속도 배율 |
|--------|-------|-----------|-------------|
| 1-5 | 3-8 | 1.0x | 1.0x |
| 6-10 | 6-12 | 1.2x | 1.1x |
| 11-20 | 10-18 | 1.5x | 1.2x |
| 21+ | 15-25 | 2.0x | 1.3x |

### 3.3 파워업 확률

| 파워업 | 확률 | 효과 |
|--------|-----|------|
| 멀티샷 | 25% | 3방향 발사 |
| 피어싱 | 20% | 관통 (2회) |
| 호밍 | 15% | 가장 가까운 적 방향 유도 |
| 빅샷 | 20% | 탄환 2배 크기 |
| 속사 | 20% | 쿨다운 30% 감소 |

### 3.4 점수 시스템

| 행동 | 점수 |
|-----|------|
| Drifter 처치 | 10 |
| Orbiter 처치 | 25 |
| Swooper 처치 | 30 |
| Tank 처치 | 100 |
| 웨이브 클리어 보너스 | 웨이브 × 50 |
| 콤보 보너스 | 연속 × 5 |

---

## 4. 기술 구현 사항

### 4.1 렌더링 최적화

```javascript
// 오브젝트 풀링
class ObjectPool {
  constructor(createFn, initialSize = 50) {
    this.pool = [];
    this.createFn = createFn;
    for (let i = 0; i < initialSize; i++) {
      this.pool.push(createFn());
    }
  }
  
  get() {
    return this.pool.length > 0 ? this.pool.pop() : this.createFn();
  }
  
  release(obj) {
    obj.active = false;
    this.pool.push(obj);
  }
}

// 탄환 풀
const bulletPool = new ObjectPool(() => ({
  x: 0, y: 0, vx: 0, vy: 0,
  radius: 8, damage: 1, active: false
}), 100);
```

### 4.2 사운드 시스템

```javascript
// Web Audio API 사운드 생성
function createShootSound(audioCtx) {
  const oscillator = audioCtx.createOscillator();
  const gainNode = audioCtx.createGain();
  
  oscillator.type = 'square';
  oscillator.frequency.setValueAtTime(880, audioCtx.currentTime);
  oscillator.frequency.exponentialRampToValueAtTime(220, audioCtx.currentTime + 0.1);
  
  gainNode.gain.setValueAtTime(0.3, audioCtx.currentTime);
  gainNode.gain.exponentialRampToValueAtTime(0.01, audioCtx.currentTime + 0.1);
  
  oscillator.connect(gainNode);
  gainNode.connect(audioCtx.destination);
  
  oscillator.start();
  oscillator.stop(audioCtx.currentTime + 0.1);
}
```

### 4.3 파티클 시스템

```javascript
class Particle {
  constructor(x, y, color) {
    this.x = x;
    this.y = y;
    this.vx = (Math.random() - 0.5) * 200;
    this.vy = (Math.random() - 0.5) * 200;
    this.life = 1.0;
    this.decay = 2 + Math.random();
    this.color = color;
    this.size = 3 + Math.random() * 3;
  }
  
  update(dt) {
    this.x += this.vx * dt;
    this.y += this.vy * dt;
    this.life -= this.decay * dt;
    this.size *= 0.98;
  }
  
  draw(ctx) {
    ctx.globalAlpha = this.life;
    ctx.fillStyle = this.color;
    ctx.beginPath();
    ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
    ctx.fill();
    ctx.globalAlpha = 1;
  }
}
```

---

## 5. 에셋 명세

### 5.1 그래픽 에셋 (Canvas 생성)

| 요소 | 크기 | 설명 |
|-----|------|------|
| 행성 | 80px | 그라디언트 원 + 글로우 |
| 캐논 | 40px | 삼각형 포탑 |
| 탄환 | 8-16px | 발광 원 |
| 적 | 20-40px | 타입별 다각형 |
| 궤도선 | - | 점선 원 |

### 5.2 사운드 에셋 (Web Audio 생성)

| 효과 | 파형 | 주파수 |
|-----|------|--------|
| 발사 | Square | 880→220Hz |
| 피격 | Sine | 440→110Hz |
| 폭발 | Noise | - |
| 웨이브 클리어 | Triangle | 상승 아르페지오 |
| 파워업 | Sine | 523→1046Hz |

---

## 6. 상태 관리

```javascript
const GameState = {
  MENU: 'menu',
  PLAYING: 'playing',
  WAVE_CLEAR: 'wave_clear',
  POWERUP_SELECT: 'powerup_select',
  GAME_OVER: 'game_over',
  PAUSED: 'paused'
};

const gameData = {
  state: GameState.MENU,
  score: 0,
  wave: 1,
  combo: 0,
  bullets: [],
  enemies: [],
  particles: [],
  powerups: [],
  playerStats: {
    fireRate: 0.5,
    bulletSpeed: 400,
    bulletDamage: 1,
    bulletSize: 8,
    multishot: 1,
    piercing: 0,
    homing: 0
  }
};
```

---

## 7. 세션 저장

```javascript
// 로컬스토리지 저장
function saveHighScore(score) {
  const highScore = localStorage.getItem('orbitStriker_highScore') || 0;
  if (score > highScore) {
    localStorage.setItem('orbitStriker_highScore', score);
    return true; // 신기록
  }
  return false;
}

function loadHighScore() {
  return parseInt(localStorage.getItem('orbitStriker_highScore')) || 0;
}
```

---

*작성일: 2026-01-28*
*버전: 1.0*
