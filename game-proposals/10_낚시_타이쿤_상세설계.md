# 🎣 낚시 타이쿤 - 상세설계서

## 1. 게임 시스템 상세

### 1.1 낚시 시스템

#### 낚시 프로세스 플로우
```
[1. 캐스팅]
터치 홀드 → 게이지 충전 → 릴리즈
    │
    ├─ 게이지 50% 이하: 근거리 (커먼 위주)
    ├─ 게이지 50-80%: 중거리 (언커먼 확률 ↑)
    └─ 게이지 80%+: 원거리 (레어 확률 ↑)

[2. 대기]
찌가 물에 떠있음 → 물고기 접근 (1-30초)
    │
    └─ 찌 움직임: 미세 → 강하게 당김

[3. 챔질]
찌가 강하게 당겨질 때 터치
    │
    ├─ Perfect (±0.2초): 희귀도 보너스 +30%
    ├─ Good (±0.5초): 희귀도 보너스 +10%
    └─ Miss (>0.5초): 물고기 도망

[4. 릴링]
물고기 HP를 0으로 만들기
    │
    ├─ 당기기: 물고기 HP 감소, 라인 텐션 증가
    ├─ 풀기: 물고기 HP 회복, 라인 텐션 감소
    └─ 라인 터짐: 텐션 100% 도달 시 실패
```

#### 챔질 시스템
```csharp
public class HookingSystem : MonoBehaviour {
    public float perfectWindow = 0.2f;
    public float goodWindow = 0.5f;
    
    private float biteTime;       // 물고기가 문 시간
    private bool isBiting = false;
    
    public void OnFishBite() {
        isBiting = true;
        biteTime = Time.time;
        
        // 찌 애니메이션
        bobberAnimator.Play("StrongPull");
        
        // 진동
        Handheld.Vibrate();
    }
    
    public HookResult TryHook() {
        if (!isBiting) return HookResult.Miss;
        
        float reactionTime = Time.time - biteTime;
        
        if (reactionTime <= perfectWindow) {
            return HookResult.Perfect;
        } else if (reactionTime <= goodWindow) {
            return HookResult.Good;
        }
        return HookResult.Miss;
    }
}
```

#### 릴링 시스템
```csharp
public class ReelingSystem : MonoBehaviour {
    public float baseTensionIncrease = 5f;
    public float tensionDecreaseRate = 3f;
    public float maxTension = 100f;
    
    private float currentTension = 0f;
    private Fish hookedFish;
    
    void Update() {
        if (hookedFish == null) return;
        
        // 물고기 저항 (랜덤 방향)
        if (hookedFish.IsResisting()) {
            currentTension += hookedFish.resistForce * Time.deltaTime;
        }
        
        // 자연 텐션 감소
        currentTension -= tensionDecreaseRate * Time.deltaTime;
        currentTension = Mathf.Clamp(currentTension, 0, maxTension);
        
        // 라인 터짐 체크
        if (currentTension >= maxTension) {
            OnLineBroken();
        }
    }
    
    public void OnReelSwipe(Vector2 direction) {
        // 당기기 - 물고기 HP 감소, 텐션 증가
        float damage = CalculateDamage(direction);
        hookedFish.TakeDamage(damage);
        
        // 물고기 저항 방향과 반대면 텐션 증가
        float tensionAdd = baseTensionIncrease;
        if (Vector2.Dot(direction, hookedFish.resistDirection) < 0) {
            tensionAdd *= 1.5f;
        }
        currentTension += tensionAdd;
    }
}
```

### 1.2 물고기 시스템

#### 물고기 데이터 구조
```csharp
[CreateAssetMenu(fileName = "Fish", menuName = "Fishing/Fish Data")]
public class FishData : ScriptableObject {
    [Header("기본 정보")]
    public string fishId;
    public string fishName;
    public string description;
    public Sprite sprite;
    public Sprite silhouette;  // 수족관 미발견 상태
    
    [Header("등급")]
    public FishRarity rarity;
    public int basePrice;
    public int exhibitValue;   // 수족관 전시 가치
    
    [Header("출현 조건")]
    public FishingSpot[] habitats;
    public TimeOfDay[] activeTimes;
    public Weather[] preferredWeather;
    public float baseSpawnRate;
    
    [Header("낚시 난이도")]
    public float hookDifficulty;  // 챔질 윈도우 감소
    public float hp;
    public float resistForce;
    public float resistInterval;
    
    [Header("크기")]
    public float minSize;  // cm
    public float maxSize;
}

public enum FishRarity {
    Common,     // 60%
    Uncommon,   // 25%
    Rare,       // 10%
    Epic,       // 4%
    Legendary   // 1%
}
```

#### 물고기 스폰 확률 계산
```csharp
public class FishSpawner : MonoBehaviour {
    public FishData SelectFish(FishingSpot spot, Equipment equipment) {
        List<FishData> availableFish = GetAvailableFish(spot);
        
        // 가중치 계산
        float totalWeight = 0f;
        Dictionary<FishData, float> weights = new Dictionary<FishData, float>();
        
        foreach (var fish in availableFish) {
            float weight = fish.baseSpawnRate;
            
            // 장비 보너스
            weight *= (1f + equipment.rarityBonus);
            
            // 미끼 보너스
            if (bait.targetRarity == fish.rarity) {
                weight *= 2f;
            }
            
            // 날씨/시간 보너스
            if (fish.preferredWeather.Contains(currentWeather)) {
                weight *= 1.5f;
            }
            if (fish.activeTimes.Contains(currentTime)) {
                weight *= 1.3f;
            }
            
            weights[fish] = weight;
            totalWeight += weight;
        }
        
        // 가중치 기반 랜덤 선택
        float random = Random.Range(0, totalWeight);
        float cumulative = 0f;
        
        foreach (var pair in weights) {
            cumulative += pair.Value;
            if (random <= cumulative) {
                return pair.Key;
            }
        }
        
        return availableFish[0]; // 폴백
    }
}
```

### 1.3 수족관 시스템

#### 수족관 데이터 구조
```csharp
[System.Serializable]
public class Aquarium {
    public string name;
    public int level;
    public List<ExhibitedFish> exhibits;
    public List<Facility> facilities;
    public float reputation;      // 0-100
    public int dailyVisitors;
}

[System.Serializable]
public class ExhibitedFish {
    public string fishId;
    public float size;           // 실제 잡은 크기
    public int tankIndex;
    public DateTime addedAt;
}

[System.Serializable]
public class Facility {
    public FacilityType type;
    public int level;
    public Vector2Int position;
}

public enum FacilityType {
    ExhibitTank,    // 전시 수조
    Restaurant,     // 레스토랑
    GiftShop,       // 기념품샵
    TouchPool,      // 체험존
    Theater,        // 해양 극장
    Entrance        // 입구
}
```

#### 수익 계산 시스템
```csharp
public class AquariumIncomeManager : MonoBehaviour {
    public int CalculateHourlyIncome(Aquarium aquarium) {
        // 1. 입장료 수입
        int ticketIncome = CalculateTicketIncome(aquarium);
        
        // 2. 부대시설 수입
        int facilityIncome = CalculateFacilityIncome(aquarium);
        
        // 3. 평판 보너스
        float reputationMultiplier = 1f + (aquarium.reputation / 100f);
        
        return (int)((ticketIncome + facilityIncome) * reputationMultiplier);
    }
    
    private int CalculateTicketIncome(Aquarium aquarium) {
        int baseTicketPrice = 10;
        float exhibitValue = 0f;
        
        foreach (var exhibit in aquarium.exhibits) {
            FishData fish = GetFishData(exhibit.fishId);
            exhibitValue += fish.exhibitValue;
            
            // 크기 보너스 (기록 물고기)
            if (exhibit.size >= fish.maxSize * 0.9f) {
                exhibitValue *= 1.5f;
            }
        }
        
        int ticketPrice = baseTicketPrice + (int)(exhibitValue / 10);
        return ticketPrice * aquarium.dailyVisitors / 24;
    }
    
    private int CalculateFacilityIncome(Aquarium aquarium) {
        int income = 0;
        
        foreach (var facility in aquarium.facilities) {
            int baseIncome = GetFacilityBaseIncome(facility.type);
            float levelMultiplier = 1f + (facility.level * 0.2f);
            income += (int)(baseIncome * levelMultiplier);
        }
        
        return income;
    }
}
```

### 1.4 장비 시스템

#### 장비 데이터
```csharp
[System.Serializable]
public class Equipment {
    public string id;
    public string name;
    public EquipmentType type;
    public EquipmentRarity rarity;
    public int level;
    
    // 스탯
    public float castDistance;     // 캐스팅 거리 보너스
    public float rarityBonus;      // 희귀 물고기 확률
    public float tensionReduction; // 텐션 증가 감소
    public float damage;           // 릴링 데미지
}

public enum EquipmentType {
    Rod,    // 낚싯대
    Reel,   // 릴
    Line,   // 낚싯줄
    Hook    // 낚싯바늘
}

// 장비 세트
public static class EquipmentSets {
    public static readonly EquipmentSet[] Sets = {
        new EquipmentSet {
            name = "초보자 세트",
            bonus = "캐스팅 거리 +10%",
            requiredItems = new[] { "basic_rod", "basic_reel" }
        },
        new EquipmentSet {
            name = "프로 앵글러",
            bonus = "레어 확률 +20%",
            requiredItems = new[] { "pro_rod", "pro_reel", "pro_line" }
        }
    };
}
```

#### 미끼 시스템
```csharp
[System.Serializable]
public class Bait {
    public string id;
    public string name;
    public BaitType type;
    public int quantity;
    public int price;
    
    // 효과
    public FishRarity targetRarity;  // 이 등급 확률 2배
    public float waitTimeReduction;  // 대기시간 감소
}

public enum BaitType {
    Worm,           // 지렁이 (무료, 무제한)
    Shrimp,         // 새우 (커먼/언커먼 ↑)
    SmallFish,      // 작은 물고기 (레어 ↑)
    GoldenBait,     // 황금 미끼 (에픽 ↑)
    LegendaryBait   // 전설 미끼 (레전더리 ↑)
}
```

### 1.5 낚시터 시스템

#### 낚시터 데이터
```csharp
[CreateAssetMenu(fileName = "FishingSpot", menuName = "Fishing/Spot Data")]
public class FishingSpotData : ScriptableObject {
    public string spotId;
    public string spotName;
    public string description;
    public Sprite background;
    public AudioClip ambientSound;
    
    public FishData[] availableFish;
    public int unlockCost;
    public int minLevel;
    
    public WeatherType[] possibleWeather;
    public float[] weatherProbabilities;
}

// 낚시터 목록
// 🏖️ 해변: 기본, 무료
// 🌊 심해: 레벨 10, 5,000 골드
// 🏞️ 강: 레벨 5, 2,000 골드
// ❄️ 북극: 레벨 20, 15,000 골드
// 🌴 열대: 레벨 15, 10,000 골드
```

---

## 2. UI/UX 설계

### 2.1 낚시 화면 (세로모드)
```
┌────────────────────────────────────┐
│ ⏸  🏖️ 해변    ☀️ 맑음   🕐 낮    │ ← 상단 정보
├────────────────────────────────────┤
│                                    │
│     ～～～～～～～～～～～～～     │
│   ～～～～～～⚫～～～～～～～    │ ← 물 + 찌
│     ～～～～～～～～～～～～～     │   (물결 애니메이션)
│                                    │
│         [수면 아래 실루엣]         │ ← 물고기 힌트 (선택)
│                                    │
├────────────────────────────────────┤
│                                    │
│             🧑‍🎣                   │ ← 캐릭터
│         ────────────              │   (낚싯대 들고 있음)
│                                    │
├────────────────────────────────────┤
│ 장비: 카본 낚싯대 🎣               │
│ 미끼: 새우 (x15) 🦐                │
│                                    │
│ [████████████░░░] 캐스팅          │ ← 캐스팅 게이지
│                                    │
│        [HOLD TO CAST]              │
└────────────────────────────────────┘
```

### 2.2 릴링 미니게임 화면
```
┌────────────────────────────────────┐
│      🐟 고등어 (커먼)              │ ← 물고기 정보
│      크기: 32cm                    │
├────────────────────────────────────┤
│                                    │
│    [물고기 HP 바]                  │
│    ████████░░░░░░░░ 65%           │
│                                    │
│         🐟←←                      │ ← 물고기 위치 + 저항 방향
│    ════════════════════           │   (좌우로 움직임)
│                                    │
│    [라인 텐션]                     │
│    ████░░░░░░░░░░░░ 25%           │
│    ⚠️ 텐션 100% = 라인 끊어짐!     │
│                                    │
├────────────────────────────────────┤
│                                    │
│         [SWIPE TO REEL]            │ ← 스와이프 가이드
│         ← 당기기 / 풀기 →          │
│                                    │
└────────────────────────────────────┘
```

### 2.3 수족관 화면
```
┌────────────────────────────────────┐
│ 🐠 나의 수족관  ⭐ 평판: 72       │
├────────────────────────────────────┤
│                                    │
│  [아이소메트릭 수족관 뷰]          │
│                                    │
│   🎫                    🎁        │ ← 입구    기념품샵
│      ┌──────┐  ┌──────┐          │
│      │ 🐟🐠 │  │ 🦈   │          │ ← 전시 수조들
│      └──────┘  └──────┘          │
│      ┌──────┐                    │
│      │ 🐙🐡 │     🍽️             │ ← 레스토랑
│      └──────┘                    │
│                                    │
│   👥 현재 방문객: 156명            │
│                                    │
├────────────────────────────────────┤
│ 💰 시간당 수입: 1,250 골드         │
│                                    │
│ [🐟전시] [🏗️시설] [📊통계] [⚙️]  │
└────────────────────────────────────┘
```

### 2.4 도감 화면
```
┌────────────────────────────────────┐
│ ←        물고기 도감               │
│      수집: 45/100 (45%)            │
├────────────────────────────────────┤
│ [전체] [커먼] [언커먼] [레어] [에픽] │
├────────────────────────────────────┤
│                                    │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐    │
│  │ 🐟 │ │ 🐠 │ │ ❓ │ │ 🦈 │    │
│  │고등어│ │열대어│ │ ?? │ │ 상어 │    │
│  │ ★☆ │ │ ★★ │ │    │ │ ★★★│    │
│  └────┘ └────┘ └────┘ └────┘    │
│                                    │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐    │
│  │ ❓ │ │ 🐡 │ │ ❓ │ │ ❓ │    │
│  │ ?? │ │ 복어 │ │ ?? │ │ ?? │    │
│  │    │ │ ★★ │ │    │ │    │    │
│  └────┘ └────┘ └────┘ └────┘    │
│                                    │
├────────────────────────────────────┤
│ 도감 보너스: 레어 확률 +5%         │
└────────────────────────────────────┘
```

### 2.5 메뉴 플로우
```
[메인 메뉴]
    │
    ├─→ [낚시하기]
    │       └─→ [낚시터 선택] → [낚시 화면]
    │
    ├─→ [수족관]
    │       ├─→ [전시 관리]
    │       ├─→ [시설 건설]
    │       └─→ [방문객 통계]
    │
    ├─→ [도감]
    │       └─→ [물고기 상세]
    │
    ├─→ [장비]
    │       ├─→ [낚싯대/릴/줄]
    │       ├─→ [미끼]
    │       └─→ [강화]
    │
    ├─→ [상점]
    │
    └─→ [설정]
```

---

## 3. 밸런싱 수치

### 3.1 물고기 등급별 수치
| 등급 | 스폰 확률 | 기본 가격 | 전시 가치 | 챔질 윈도우 |
|------|----------|----------|----------|------------|
| 커먼 | 60% | 50 | 5 | 0.5초 |
| 언커먼 | 25% | 150 | 15 | 0.4초 |
| 레어 | 10% | 500 | 50 | 0.3초 |
| 에픽 | 4% | 2,000 | 200 | 0.2초 |
| 레전더리 | 1% | 10,000 | 1,000 | 0.15초 |

### 3.2 낚시터별 물고기 분포
| 낚시터 | 커먼 | 언커먼 | 레어 | 에픽 | 레전더리 |
|--------|------|--------|------|------|---------|
| 해변 | 70% | 20% | 8% | 1.8% | 0.2% |
| 심해 | 40% | 30% | 20% | 8% | 2% |
| 강 | 65% | 25% | 8% | 1.5% | 0.5% |
| 북극 | 50% | 25% | 15% | 8% | 2% |
| 열대 | 55% | 28% | 12% | 4% | 1% |

### 3.3 장비 스탯
| 장비 등급 | 캐스팅 거리 | 희귀 보너스 | 텐션 감소 | 가격 |
|----------|------------|------------|----------|------|
| 일반 | +0% | +0% | +0% | 기본 |
| 고급 | +15% | +10% | +10% | 1,000 |
| 희귀 | +30% | +20% | +20% | 5,000 |
| 에픽 | +50% | +35% | +35% | 15,000 |
| 전설 | +80% | +50% | +50% | 50,000 |

### 3.4 미끼 효과
| 미끼 | 가격 | 타겟 등급 | 대기시간 |
|------|------|----------|----------|
| 지렁이 | 무료 | - | 기본 |
| 새우 | 50 | 커먼 2배 | -20% |
| 작은 물고기 | 200 | 레어 2배 | -30% |
| 황금 미끼 | 1,000 | 에픽 2배 | -40% |
| 전설 미끼 | 5,000 | 레전더리 2배 | -50% |

### 3.5 수족관 수익
| 시설 | 기본 수입/시간 | 레벨당 증가 | 건설 비용 |
|------|--------------|------------|----------|
| 전시 수조 | 물고기 가치 기반 | +10% | 2,000 |
| 레스토랑 | 100 | +15% | 5,000 |
| 기념품샵 | 80 | +12% | 3,000 |
| 체험존 | 150 | +20% | 8,000 |
| 해양 극장 | 200 | +25% | 15,000 |

### 3.6 오프라인 수익
```
최대 축적 시간: 12시간
기본 수익률: 시간당 수입의 100%
오프라인 수익률: 시간당 수입의 50%
광고 시청 보너스: 2배 (최종 100%)
VIP 패스: 오프라인도 100%
```

### 3.7 레벨 시스템
| 레벨 | 필요 경험치 | 해금 |
|------|------------|------|
| 1 | 0 | 해변 |
| 5 | 500 | 강 |
| 10 | 2,000 | 심해 |
| 15 | 5,000 | 열대 |
| 20 | 10,000 | 북극 |
| 25 | 20,000 | 도전 모드 |
| 30 | 50,000 | 전설 낚싯대 해금 |

---

## 4. 기술 구현 사항

### 4.1 물 셰이더
```csharp
// URP Shader Graph 또는 Flat Kit 활용
// 물 표현 요소:
// - 파동 애니메이션 (Vertex Displacement)
// - 반사 (Screen Space Reflection)
// - 굴절 (Refraction)
// - 폼/거품 (Edge Detection)

// 찌 물결 효과
public class BobberRipple : MonoBehaviour {
    public Material waterMaterial;
    public float rippleStrength = 0.5f;
    public float rippleDuration = 2f;
    
    public void CreateRipple(Vector3 position) {
        StartCoroutine(RippleEffect(position));
    }
    
    IEnumerator RippleEffect(Vector3 pos) {
        float elapsed = 0f;
        waterMaterial.SetVector("_RippleCenter", pos);
        
        while (elapsed < rippleDuration) {
            float t = elapsed / rippleDuration;
            waterMaterial.SetFloat("_RippleRadius", t * 5f);
            waterMaterial.SetFloat("_RippleStrength", rippleStrength * (1 - t));
            elapsed += Time.deltaTime;
            yield return null;
        }
    }
}
```

### 4.2 물고기 AI
```csharp
public class FishAI : MonoBehaviour {
    public enum FishState { Idle, Approaching, Biting, Hooked, Escaping }
    
    public FishData data;
    private FishState state = FishState.Idle;
    private Vector3 targetPosition;
    
    void Update() {
        switch (state) {
            case FishState.Idle:
                // 랜덤 수영
                Swim();
                CheckBait();
                break;
                
            case FishState.Approaching:
                // 미끼로 접근
                MoveTowardsBait();
                break;
                
            case FishState.Hooked:
                // 저항
                Resist();
                break;
        }
    }
    
    private void Resist() {
        // 주기적으로 방향 변경
        if (Time.time > nextResistTime) {
            resistDirection = Random.insideUnitCircle.normalized;
            nextResistTime = Time.time + data.resistInterval;
            
            // Feel 피드백
            MMFeedbacks resistFeedback = GetComponent<MMFeedbacks>();
            resistFeedback?.PlayFeedbacks();
        }
        
        // 현재 방향으로 힘 가하기
        transform.position += (Vector3)resistDirection * data.resistForce * Time.deltaTime;
    }
}
```

### 4.3 도감 시스템
```csharp
public class CollectionManager : MonoBehaviour {
    private Dictionary<string, CollectionEntry> collection;
    
    [System.Serializable]
    public class CollectionEntry {
        public string fishId;
        public bool discovered;
        public int catchCount;
        public float maxSize;
        public float minSize;
        public DateTime firstCaught;
    }
    
    public void OnFishCaught(string fishId, float size) {
        if (!collection.ContainsKey(fishId)) {
            // 최초 발견
            collection[fishId] = new CollectionEntry {
                fishId = fishId,
                discovered = true,
                catchCount = 1,
                maxSize = size,
                minSize = size,
                firstCaught = DateTime.Now
            };
            
            // 발견 연출
            ShowDiscoveryAnimation(fishId);
            
            // 도감 보너스 재계산
            RecalculateCollectionBonus();
        } else {
            var entry = collection[fishId];
            entry.catchCount++;
            entry.maxSize = Mathf.Max(entry.maxSize, size);
            entry.minSize = Mathf.Min(entry.minSize, size);
        }
    }
    
    public float GetCollectionBonus() {
        float discoveredRate = (float)collection.Count(e => e.Value.discovered) 
                              / totalFishCount;
        
        // 10% 단위로 보너스
        return Mathf.Floor(discoveredRate * 10) * 0.05f; // 최대 50% 보너스
    }
}
```

### 4.4 날씨 시스템
```csharp
public class WeatherSystem : MonoBehaviour {
    public WeatherType currentWeather;
    public float weatherChangeProbability = 0.1f; // 분당
    
    [System.Serializable]
    public class WeatherEffect {
        public WeatherType type;
        public ParticleSystem particles;
        public AudioClip ambientSound;
        public float fishActivityModifier;
        public float[] rarityModifiers; // 등급별
    }
    
    public WeatherEffect[] weatherEffects;
    
    void Start() {
        InvokeRepeating(nameof(CheckWeatherChange), 60f, 60f);
    }
    
    private void CheckWeatherChange() {
        if (Random.value < weatherChangeProbability) {
            ChangeWeather(GetRandomWeather());
        }
    }
    
    private void ChangeWeather(WeatherType newWeather) {
        // 기존 효과 페이드아웃
        StartCoroutine(TransitionWeather(currentWeather, newWeather));
    }
}

public enum WeatherType {
    Sunny,      // 맑음 - 기본
    Cloudy,     // 흐림 - 언커먼 ↑
    Rainy,      // 비 - 레어 ↑
    Stormy,     // 폭풍 - 에픽 ↑
    Night       // 밤 - 특정 물고기 출현
}
```

### 4.5 오프라인 진행
```csharp
public class OfflineManager : MonoBehaviour {
    public void OnApplicationPause(bool paused) {
        if (paused) {
            SaveLastPlayTime();
        } else {
            CalculateOfflineProgress();
        }
    }
    
    private void CalculateOfflineProgress() {
        DateTime lastPlay = LoadLastPlayTime();
        TimeSpan offline = DateTime.Now - lastPlay;
        
        // 최대 12시간
        float hours = Mathf.Min((float)offline.TotalHours, 12f);
        
        // 수족관 수입 계산 (50% 효율)
        int hourlyIncome = aquariumManager.GetHourlyIncome();
        int offlineGold = (int)(hourlyIncome * hours * 0.5f);
        
        // 방문객 계산
        int visitors = (int)(aquariumManager.GetHourlyVisitors() * hours);
        
        ShowOfflineRewardPopup(offlineGold, visitors);
    }
}
```

### 4.6 데이터 저장
```csharp
[System.Serializable]
public class FishingTycoonSaveData {
    // 플레이어
    public int gold;
    public int gems;
    public int level;
    public int experience;
    
    // 인벤토리
    public List<CaughtFish> inventory;
    public List<Equipment> equipment;
    public Dictionary<string, int> baitInventory;
    
    // 수족관
    public Aquarium aquarium;
    
    // 도감
    public Dictionary<string, CollectionEntry> collection;
    
    // 진행
    public List<string> unlockedSpots;
    public DateTime lastPlayTime;
    public Dictionary<string, bool> achievements;
}
```

### 4.7 성능 최적화

#### 타겟 성능
| 항목 | 목표 |
|------|------|
| FPS | 60 FPS (낚시), 30 FPS (수족관) |
| 메모리 | <400MB |
| APK 크기 | <120MB |
| 배터리 | 저전력 모드 지원 |

#### 최적화 기법
```
1. 물고기 오브젝트 풀링
2. 물 셰이더 LOD
3. 수족관 물고기 - 원거리 시 스프라이트 교체
4. 파티클 - GPU 인스턴싱
5. UI 아틀라스 - 물고기 스프라이트
6. 오디오 - 스트리밍 (환경음)
```

---

## 5. QA 체크리스트

### 5.1 낚시 메카닉
- [ ] 캐스팅 게이지 정확성
- [ ] 챔질 타이밍 판정
- [ ] 릴링 텐션 계산
- [ ] 물고기 저항 패턴
- [ ] 라인 끊어짐 조건

### 5.2 물고기 시스템
- [ ] 등급별 스폰 확률
- [ ] 날씨/시간 영향
- [ ] 미끼 효과 적용
- [ ] 크기 범위 정확성
- [ ] 도감 등록 정상 작동

### 5.3 수족관
- [ ] 수익 계산 정확성
- [ ] 시설 배치
- [ ] 방문객 시뮬레이션
- [ ] 오프라인 수익

### 5.4 UI/UX
- [ ] 낚시 피드백 (진동, 사운드)
- [ ] 물 시각 효과
- [ ] 해상도별 UI
- [ ] 터치 반응성

### 5.5 성능
- [ ] 수족관 물고기 50마리 시 FPS
- [ ] 메모리 누수
- [ ] 배터리 소모
- [ ] 저사양 기기 테스트

---

*작성일: 2026년 1월 27일*
*버전: 1.0*
